# fastloop/work — runtime scratch (NOT committed beyond this README)

Job outputs (`fastloop.<jobid>.out`, `build_minideck.<jobid>.out`), per-run
leg directories (`check.<jobid>/{p1,shard4}/`), and the one-time deck build
dir land here. Nothing in this directory is a record: verdicts go to
CLAIMS.md, pins to `../pins_mini.json`. Safe to delete except this file.
