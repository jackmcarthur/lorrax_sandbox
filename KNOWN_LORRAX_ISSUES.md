# Known LORRAX issues — live register

One row per broken/deferred item in the main drivers. Append; mark FIXED with
the commit rather than deleting. Evidence = scorecard section or job id.
SUP = needs owner decision (physics convention or release call); everything
else is agent-actionable. Replaces the archived KNOWN_SANDBOX_ERRORS.

## centroids (kmeans_cli)
| issue | evidence | smallest fix | status |
|---|---|---|---|
| psi-at-centroids sharded on ONE mesh axis (sqrt(P) scaling) | BD.4; wfn_transforms.py:1933,2002 | both-axes mu-shard | open |
| weight field built replicated on full r-grid (7 s unscaled) | BD.4 (7.1 s at P=1, 7.0 s at P=16, 300 bands); b600/P=64 job 7885315: `setup.weight` 19.99 s = **61.4% of the whole kmeans driver** (32.58 s recorded) at 600 bands / 4800c / 64 ranks — still zero P-scaling, and it now dominates every other section (Lloyd 4.36 s, prune 6.01 s) | grid-shard like Lloyd | FIXED 920665f — band-split, not grid-shard: the band×k sum is separable, so each rank takes a disjoint set of band chunks and ONE psum returns the whole (the split also cuts the per-rank ψ read by P; loader is process-local eager h5py, so disjoint reads are safe). Two measured corrections to the obvious implementation: (a) the 4 GB chunk budget alone yields 3 chunks at 600 bands, capping the split at 3 ranks — 0.70x, SLOWER (job 7885968) — so chunks are now also sized ≥1 per rank; (b) per-chunk psum is bit-identical to the serial left-fold but costs 0.85 s per call at this payload, 12x slower on the sparse leg and ~50 s of pure latency at P=64 (job 7885969), so it reduces once. Reducing once regroups additions: weight moves 1.6e-16 rel (1 ulp) — smaller than the 8.2e-16 that re-chunking alone costs. Gate is centroid-set identity, and it holds: `kmeans_cli 400 --orbit --seed 0 --weight-bands 0:600` at P=4 gives centroids_frac_396.txt BYTE-FOR-BYTE identical vs frozen 1fc2759 (job 7885975). 2.85–3.30x on the sweep at P=4/4 chunks; P=1 keeps the original fused loop verbatim. RESIDUAL: `symmetrize_on_grid` (12 ops) and the Lloyd/prune sections are untouched; re-measure the driver split at P=64 |
| pair tensors col-blocked only on single-device path | BD.4; pivoted_cholesky.py:800 | extend col-blocking to multi-device | open |
| P>1 thread-main refusal | BD.3, job 7884867 | prepare_mesh routing | FIXED 24e4dc3; subsumed by e97e8ed (mesh + warm-up now from initialize_communicator_stack) |
| `--density-mode current` INFLATES the orbit-closed count by 4.2-4.6% at mu~1600, so the delivered count cannot be aimed directly: 6-attempt ladder gave 1600->1667, 1536->1604, 1528->1591, 1544->1606, 1520->1579, 1552->1617 (the charge channel DEFLATES 0.5% at 4800). None of the six landed on a 64- or 8-divisible count, so the ~1/6 divisibility hit rate the transverse precedent suggested did not materialise in six draws. Divisibility was run-losing while the phdf5 padded-rank-slice write defect stood (cross-cutting row, FIXED d935ce7 + job 7886458, which ran mu_T=1604 to rc=0); it is now a PADDING-EFFICIENCY matter only — 1604 pads to 1664, i.e. 60 wasted rows against 1591's 9 | job 7885953 ladder; /scratch2/08271/jackmc/b600_bispinor_p64/run_centT/cand_*.txt; RESULTS.md S1 | teach `kmeans_cli` a `--pad-safe P` flag that rejects a delivered count with `pad >= ceil(mu/P)` (and retries), so the ladder cannot hand the writer a doomed set | open (low) |

## dipole / kin-ion
| issue | evidence | smallest fix | status |
|---|---|---|---|
| replicated (nk,3,nb,nb)/(nk,nb,nb) gathers; only consumer is rank-0 write | BD.4; get_dipole_mtxels.py:912, kin_ion_io.py:453 | gather_k_blocks owner-write mode | FIXED 7d939be — the row was STALE: `gather_k_blocks(..., owner_only=True)` is live at both call sites (get_dipole_mtxels.py:920, kin_ion_io.py:712/744) and `docs/dev/large_nmu_operation.md` item 6 already recorded it. Measured at b600/P=64 (job 7885315, 600 bands): per-rank VmHWM is one-hot exactly as owner-write predicts — dipole max 2.10 GiB / mean 1.25 / min 0.47; kin-ion max 2.15 / mean 0.66 / min 0.46. No rank but 0 carries the nb² table |
| full WFN.h5 h5py read PER RANK at P>1 | BD.2 banner; b600/P=64 job 7885316 quantifies it on the GW centroid path: `load_centroids.loader_load` 0.19 s (b300/P=16) → 5.05 s (b600/P=64) = **26.5x**, against ~8x more bytes (2x bands × 4x ranks, each rank reading the whole window of a 609 MB WFN) — superlinear Lustre contention, the worst-scaling term in the entire chain | let auto pick phdf5 collective read | open — absolute cost still small (5 s) but the exponent is wrong; it degrades faster than P grows |
| dipole anti-scales at small decks (0.8x at P=16) | BD.2 | startup amortization only; k-sweep itself scales | wontfix-small-decks |

## gw
| issue | evidence | smallest fix | status |
|---|---|---|---|
| zeta-fit factor: nq x mu^3 eigh on EVERY rank, zero P-scaling; 64% of GW wall at 2979c | BC, job 7884656 (cholesky 105.1 s) | q-parallel factor plan, auto above size threshold | FIXED 854af1f — folded INTO the replicated plan as its P>1 schedule (not a third resolution; W-solve LOCAL-plan idiom), auto above nq·mu³≥5e9, LORRAX_ZETA_QPARALLEL=0/1 override; b300 8x2 A/B job 7885024: cholesky 104.4→11.8 s (8.9x), GW wall 336→215 s, eqp/sigma parity exact-0 vs control AND vs 7884656; bit-identity gate max_abs=0.0; CLAIMS 23 |
| chi0_W_probe re-runs chi+W nearly in full (~9.6 s); at b600/P=64 it now costs **1.44x the real screening pass** (16.35 vs 11.33 s, job 7885316) because the probe Dyson solves the FULL BZ (16 q) while the real pass solves the IBZ wedge (10 q) and neither gets P-parallelism past nq — probe `W.exec` 3.99→11.92 s (2.99x) vs the real `W.exec` 4.05→5.21 s. The shipped `ppm_probe_chi_reuse=auto` opt-in CANNOT close this: it folds the χ sweep (3.88 s here), not the 11.92 s Dyson solve, which its own row already calls frozen-golden | BC; b600 job 7885316 | reuse the real pass | opt-in shipped 64b17c3 (`ppm_probe_chi_reuse=auto`, default off): probe χ₀ folds into ONE fused τ sweep on static+k augmented nodes — weights-only refit on the static nodes is provably insufficient (probe integrand = Laplace transform of cos ωp·t; 2.6e-4 vs dedicated 1.3e-6, job 7885097). Premise correction: only the χ sweep (~3.8 s of the 9.6) is foldable — the probe's full-BZ Dyson solve is its own (frozen-golden). b300 A/B job 7885109: probe row 6.78→3.56 s but greedy-Lawson planning costs 1.79 s host ⇒ screening NETS +1.2 s at b300 scale; pays only where per-node χ cost dominates (larger μ). Deltas eqp ≤8.7e-5 eV, sigma_mnk ≤1.04e-4 eV (CLAIMS 27). RESIDUAL (open): cheapen the planner (coarse grid / fewer Lawson iters in the greedy scan) before any default-flip/re-pin owner call |
| sigma.exec 88% d2h_wait — host accumulator not overlapped | BC | double-buffer the tau d2h | FIXED 8a9a21c + premise REFUTED: the loop was already lag-2 overlapped (2026-07-28) and d2h_wait is the τ-kernel DEVICE wall surfacing in the drain (the in-code comment warned exactly this) — the stage is device-bound. Real slack was the per-window boundary flush (16 drains, 5.77 s at BC); deque now persists across windows (one flush per branch, FIFO, bit-identical): b300 leg A EXACT-0 vs 7884656 on all five outputs, sigma.exec 71.22→70.61 s, flush 2.64 s under sigma.finalize (jobs 7885105 unit bit-parity, 7885109; CLAIMS 26) |
| charge ζ-factor q-parallel fold saturates at P = nq_ibz, not at P: the local plan scatters nq whole (μ,μ) tiles over the mesh, so every rank past nq idles for the whole factor stage. b600/P=64 (nq_ibz=10): the driver itself announces `nq=10 scattered over 64 devices ... q-pad 54` — **54 of 64 ranks idle for 53.7 s = 22.4% of the GW wall**. Going P=16→64 bought this stage exactly nothing (it was already 1 q/rank at P=16), so its 11.76→53.71 s growth is pure μ³ | job 7885316 vs 7885024; isdf/core.py factor_c_q q-parallel schedule (854af1f); docs/dev/large_nmu_operation.md presents the fold with no P=nq ceiling stated | state the P/nq ceiling in the resolver announcement and in large_nmu_operation.md; consider making `auto` promote to the distributed tier when P/nq exceeds a measured ratio (at P/nq=6.4 the distributed tier is already 1.64x faster — see next row) | open |
| distributed ζ tier's benefit is documented BACKWARDS at production shape: `large_nmu_operation.md` says the tier "buys memory shape and P-scaling, not wall time", and places the wall crossover at μ=10015 on 64 ranks. Measured at μ=4775/P=64 (job 7885323 vs 7885316, one deck key changed): wall 214.8→177.6 s minus startup (**0.83x**), `zeta_fit.cholesky` 53.71→32.74 s (**0.61x**), `chunk.solve` 10.20→6.68 s — but per-rank peak VmHWM **16.24→16.22 GiB, i.e. unchanged**, even though the local tier's announced 3.69 GB/rank replicated (nq,μ,μ) gather is entirely absent from the distributed one. The peak is set by the `C_fit_one_rchunk` transient, not by the ζ gather, so removing the gather moves no memory. Crossover is governed by P/nq, not μ alone | jobs 7885316 (local) / 7885323 (distributed), /scratch2/08271/jackmc/b600_p64/RESULTS.md §6; parity local-vs-distributed 1.1–1.5e-5 eV on all four .dat outputs (κ/q = 9.98e7, the documented κ·ε gauge class) | correct the tier's wall/memory framing in large_nmu_operation.md and record the P/nq crossover; the memory claim should name the binder it actually relieves | open |
| ISDF memory planner mis-calibrated at low P — it OVER-estimates, and the over-estimate costs wall by forcing extra r-chunks. b300/P=16: `HWM estimate = 33.99 GB/dev (85% of budget)` → measured sacct MaxRSS **4.63 GiB** (7.3x over), and the 40 GB budget therefore split r into 2 chunks. b600/P=64: estimate 16.43 GB/dev → measured VmHWM 17.44 GB (**1.06x**, well calibrated) and r fits in 1 chunk. The b300 chunk_loop being 2.8x the b600 one at HALF the μ is largely this | jobs 7885024 (b300 planner block + sacct), 7885316 (b600 planner block + VmHWM) | re-fit the `C_fit_one_rchunk` binder against measured VmHWM at P=1/16/64; a 7x-high estimate silently buys chunks nobody needed | open |
| Sigma_c is PPM-only; no full-frequency cross-check | gw_config.py:306 | validation mode on bse/w_omega_chain.py | open |
| plan_gflat_chunks stops at the restart write — NO Σ-stage rows: the σ_X/σ^B G tensors (nk, ns, μ, ns, μ) + ~3 FFT transients are unplanned (charge @4775c/P=64 ≈ 4.4 GB/rank, absorbed by headroom in job 7885316; bispinor @1600T adds ~0.5–0.66 GB/rank ×9 sequential (i,j) pairs); binds at larger μ/nk or smaller P with no planner warning | gflat_memory_model.py:442 (peaks dict ends at F_tensor_write); cohsex_sigma.py:120 _convolve; memory audit job 7885325 | add Σ-stage peak rows (per channel μ) so P_min covers the Σ build | open |
| slab_io=auto -> PHDF5_FFI aborts in MPI_Init on a PMI-less bare launch (no srun) instead of demoting | CLAIMS 18, job 7884926; fastloop catch | auto probes MPI bootstrapability, announced demotion (GPU router precedent) | FIXED aef6710 (launcher-PMI check + subprocess singleton-init probe; announced demotion; jobs 7884987/7884989; CLAIMS 21) |
| gw_jax hangs indefinitely in interpreter teardown after main() at bare P=1 (only FFT/GEMM-FFI chain driver) | CLAIMS 19, job 7884928; fastloop catch | root-cause FFI/XLA:CPU teardown; interim: fastloop GW_WRAPPER os._exit | FIXED d3465cc (root cause: jax atexit clean_up destroys the XLA:CPU client and its pool shutdown deadlocks after fully-cold compile storms — reproduced 2x, job 7884989; runtime.finalize_process does the ordered teardown explicitly; GW_WRAPPER removed; CLAIMS 22) |
| transverse ζ has NO standalone factor stage: per-q LU re-factored EVERY r-chunk — on every rank under default `distributed_lu=auto` (CPU auto never picks scalapack), and re-run (though 2D-sharded) per r-chunk under `distributed_lu=scalapack`; the 854af1f q-parallel fold cannot apply (it schedules the factor stage) | isdf/core.py:2582 (passthrough), :3046 (`_ridge_indef_solve`, jnp.linalg.solve per r-chunk), ffi/scalapack/solve_lu.py (getrf per call); zeta audit 2026-08-01 | hoisted transverse factor stage (per-q `lu_factor` once per channel — same getrf bits; local plan gets the q-parallel fold + bit-identity gate idiom, distributed plan keeps pXgetrf factors block-cyclic + pXgetrs per r-chunk); design in repo docs/dev/large_nmu_operation.md | FIXED — branch `zeta-transverse-hoist` @ f695bf4 (6571c04 ffi split + 5ed31c7 hoist + 2fca4b4 CLI init-order; MERGED to fix/zq 2026-08-01 as 6f162f6 + build-gate 96d7996, conflicts resolved as content supersets against the wave-2 facade, certified job 7885137 — CLAIMS row 32): factor_c_q returns (factor, piv), solve_zeta only applies per r-chunk; local plan bit-identical to fused (unit gate exact, 5 meshes × 2 tiers × 2 schedules, job 7885122; bi4 P=4 AND bi16 P=16 production A/B EXACT-0 all outputs, job 7885126), scalapack split bit-identical (contract cell 2x2 P=4 job 7885125; production E/F EXACT-0 job 7885126), dist-vs-local gauge 0.0 .dat / 1.1e-13 eV h5; bi16 per-channel chunk.solve 4.3–12.8→1.7–2.8 s, GW wall 214.3→201.2 s; divisible transverse set centroids_T_div136 (136 pts, %8=0) at /scratch2/08271/jackmc/zeta_T_hoist/; RESIDUAL: cusolvermp (CUDA) keeps the fused per-r-chunk pair (hoist port pending); large ~272-pt divisible set not yet generated (CLAIMS 29-31) |
| ζ-fit provenance omits `distributed_zeta_solve`: a ζ fit under the distributed tier (gauge differs ~κ·ε from replicated) is silently REUSED by a replicated-tier rerun in the same dir, and vice versa | gw_init.py:186-221 (`_zeta_fit_provenance` — records charge_zeta_solve/rcond but not the tier); zeta audit 2026-08-01 | record the resolved tier string in the provenance dict (costs a one-time refit for every existing on-disk ζ — owner call on timing) | FIXED 8b02c72 (owner-approved) with NO forced refit: stamp records the gauge class ('distributed' \| 'replicated'; per_q collapses to replicated — same factor bits), schema stays 1, reader treats a MISSING key as legacy replicated (one-line notice, reuse allowed) and refits on real tier mismatch. Reader matrix proven on the real b300 legacy ζ (4 cases, job 7885105); b300 leg C: doctored stamp took the generic refit path (copied dir also changed wfn_file — 2-key diff, both named) and refit landed exact-0 on all .dat outputs vs control (job 7885109); CLAIMS 25 |
| transverse ζ solver resolver has NO capacity gate: `_resolve_solver_kind_charge._auto_pre` refuses above the replication cap and names the exact μ ceiling (16384) and the fix, but `_resolve_solver_kind_transverse` never calls `_replicate_charge_ok`/`_replicate_rank_truncate_ok` at all — the local plan allocates a whole (μ_T, μ_T) c128 tile per device with no check. Inert at μ_T=1664 (42.2 MiB tile); above μ_T≈16k a multi-node run OOMs minutes into the fit instead of refusing in seconds | isdf/core.py:1248 (`_resolve_solver_kind_transverse`, no `_replicate_*_ok` call) vs :1167-1244; parity audit 2026-08-02 | call `_replicate_rank_truncate_ok(q_batch, μ_T)` in the transverse resolver and raise with the same message shape as the charge branch | open |
| transverse distributed ζ tier is UNREACHABLE at the live production centroid count. Three compounding constraints: (a) `distributed_zeta_solve=distributed` on a transverse channel silently resolves DOWN to `per_q` (isdf/core.py:1544-1560); (b) the only distributed route, `distributed_lu=scalapack`, is never auto-picked on a CPU mesh (`_resolve_channel_ladder`, cuSOLVERMp is CUDA-only); (c) it requires μ_T_logical % p_x == % p_y == 0 at the LOGICAL extent. The b600 bispinor deck's transverse set is 1604 points and 1604 % 8 = 4, so on the certified 8x8 mesh the route refuses (explicit) or demotes (auto). At μ_T≈10k for the thousands-of-procs target the local plan needs a 1.49 GiB/rank whole tile, over budget, with no escape | isdf/core.py:1248-1354, :1544-1560; /scratch2/08271/jackmc/b600_bispinor_p64/centroids_T_b600.txt (1604 pts); CLAIMS 31 (divisible-set search ~1/6 hit rate); parity audit 2026-08-02 | merge branch `zeta-transverse-ranktrunc` @ 340686f: `transverse_zeta_solve=rank_truncate` runs pzheevd at the PADDED extent with exactly-inert pads, so the divisibility constraint does not exist on that family | open — fix IN FLIGHT (unmerged worktree /work2/08271/jackmc/frontera/lorrax_wt_ranktrunc) |
| ζ REUSE is switched off for the entire bispinor run, and the three ζ_T files carry no provenance stamp at all: `_reuse = (not cfg.bispinor) and _zeta_reuse_ok(...)` (gw_init.py:503), and `_zeta_fit_provenance` hard-codes `'vertex_mu_L': 0` and is stamped only on zeta_q.h5 — the μ_L loop (gw_init.py:722-758) never stamps. Stated reason: the reuse path returns `transverse_wfn_data=None`, which σ^B needs. Consequence: a same-dir bispinor rerun repays the whole 4-channel ζ fit, the run's dominant stage (~500-800 s projected at 4800C+1604T/600b/P=64). `restart = true` does cover it via the tensors H5, so this is re-run ergonomics, not a hard block. Separately `distributed_lu` is absent from the stamp, so a ζ_T fit under `scalapack` (block-cyclic gauge) is silently reusable by an `off` rerun | gw_init.py:142-238 (stamp), :503 (bispinor gate), :722-758 (unstamped μ_L loop); parity audit 2026-08-02 | (i) return `psi_full_y_transverse` from the restart H5 on the reuse path instead of None; (ii) stamp the three ζ_T files; (iii) add `distributed_lu` to the stamp under the existing legacy-missing-key rule | FIXED 82cefa2 (branch `fix/bispinor-zeta-reuse`, NOT merged; supersedes the earlier "open — worktree 340686f adds transverse_zeta_solve/transverse_zeta_rcond to the stamp but does not re-enable bispinor reuse"). All three sub-items done, with (i) taken a DIFFERENT and better way than the row prescribed: the reuse path does NOT read `psi_full_y_transverse` from the restart H5 — that file is a separate mechanism and need not exist — it RE-SAMPLES ψ at the transverse centroids through the same `_transverse_wfn_data` helper the fit path calls, ψ(r_μT) being a pure function of WFN.h5 + the transverse centroid file to which the ζ_T fit contributes nothing. Returning None would silently drop Σ^B (the failure 3d89885 fixed on the restart round-trip); an AST gate pins that no `return ..., None` in fit_zeta is reachable under bispinor. (ii) the three ζ_T ARE stamped and each is checked — zeta_is_done + byte-identical stamp + centroid table + ζ dataset extent — all four channels or none. (iii) `distributed_lu` is in the stamp, plus `n_rmu_transverse`, `centroids_transverse_md5` (CONTENT, not count — pattern #10) and the RESOLVED `transverse_solver_kind`; `vertex_mu_L` is no longer hard-coded 0, so a ζ_T stamped for one vertex cannot be reused as another, and `_zeta_fit_provenance` REFUSES a bispinor stamp carrying no transverse identity. MEASURED bi4/P=4 (job 7888568): fit leg 336 s → reuse leg 51 s; ζ-fit time skipped 255.7 s, replaced by 2.0 s of ψ re-sampling; `gw_jax.isdf` 264.2 → 7.8 s. EXACT-0 (0.000e+00) reuse-vs-fit on eqp0/eqp1/eqp_g0w0/sigma_diag and sigma_mnk.h5, all nine Σ^B tile traces byte-identical across legs. The b600/P=64 saving is the 318 s job 7885966 already measured (charge 181.85 + transverse 135.91) minus ~9 s of re-sampling — projected, not measured at that scale |
| the three transverse ζ fits inherit the CHARGE channel's chunk plan verbatim: `plan_gflat_chunks` runs ONCE, with μ = μ_C (gflat_memory_model.py:305) and `is_bispinor` used at exactly one line (:423, a second ζ slab in Stage E); the μ_L loop reuses `chunks['chunk_r'/'band_chunk'/'q_chunk'/'gflat_chunk_size']`. The transverse Stage-C arena is μ_T/μ_C ≈ 1/3 of planned, so ζ_T takes ~3x more r-chunks than its own memory allows — and each r-chunk re-gathers the factor (422 MiB/rank at μ_T=1664, nq=10): ~2.7 GB/rank of avoidable AllGather across 3 channels. Conservative, so no OOM; pure waste | gflat_memory_model.py:274-466, gw_init.py:1087 (single call), :700/732/734/735/746 (inherited); smoke log /scratch2/08271/jackmc/b600_bispinor_p64/logs/smoke_gw_bi.7885952.log (charge and all three transverse fits report the same "3 r-chunks"); parity audit 2026-08-02 | re-call `plan_gflat_chunks` with `meta_curr` before the μ_L loop and use its chunk_r/band_chunk | open |
| planner Stage E models ONE V_q tile at `(nq_ibz, μ_C, μ_C)/P`; the bispinor Lorentz mix holds ~27 tile-equivalents at `(nq_full, μ_T, μ_T)/P` — a 5.4x under-model (54.9 MiB/rank modelled vs 285 MiB/rank real at 1604T/P=64; 4.46 GiB/rank at P=4). Note the mix also runs at n_q_full=nk, not n_q_ibz | gflat_memory_model.py:418-425 vs v_q_bispinor.py:357-401 + common/symmetry_maps.py:602-612; parity audit 2026-08-02 | add a Stage-E' peak row sized on μ_T, n_q_full and the real tile count (see the bispinor TT Lorentz-mix row for the concurrency reduction) | open |
| a bispinor run does the whole-window ψ(G) h5py read TWICE (once per centroid set) and rebuilds `psi_G_store` FOUR times (once per ζ fit, no cross-call cache) — against 1 + 1 on the charge-only path. This multiplies the chain's worst-scaling term: measured charge b600/P=64 `load_centroids.loader_load` 5.05 s + `psi_G_store.populate.loader_load` 0.549 s ⇒ 2x5.05 + 4x0.549 = 12.3 s vs 5.6 s. The four reads differ only in `centroid_indices`; band window, k-set and sharding are byte-identical | common/wfn_transforms.py:1893/2054-2062, file_io/psi_G_store.py:392/411 (populate per construction), gw_init.py:695-701 vs :1139-1143; jobs 7885316 (charge timings); extends the open "full WFN.h5 h5py read PER RANK" row above; parity audit 2026-08-02 | memoize `load_psi_gflat_padded` on (band_range, k-set, sharding) | open |
| `zeta_rcond` is not plumbed to the transverse ζ fits — the μ_L loop omits it, so it falls to the `fit_zeta_to_h5` signature default 1e-8 and a deck that tunes the key silently tunes only the charge channel. Inert today (the hoisted LU route never reads it) but live the moment an eigh-based transverse family lands | gw_init.py:518-539 vs :728-758; isdf_fitting.py:164; parity audit 2026-08-02 | pass the key, or delete the unused parameter on the transverse call (branch 340686f introduces a separate `transverse_zeta_rcond`, which supersedes rather than fixes this) | open (latent) |

## htransform
| issue | evidence | smallest fix | status |
|---|---|---|---|
| replicated SVD family: A=(nk*nb, ns*N_mu), Vh, B_at_mu — last N_mu^2-replicated core in the chain | BD.4; htransform.py:262,356,626 | Gram-eigh of A A^H via ffi.linalg plan + mu-shard | FIXED 62ba395 — eigh(A A^H) (nk·nb square, N_mu-free) via ffi.linalg plan (deck key eigh_backend, pzheevd available), Vh/B_at_mu mu-sharded on 'y' (B fitted at true n_mu); gauge-class, measured exact-0 at file precision everywhere: fastloop 7885090 PASS both legs, b300 P=1 dft+qp and srun P=4 exact-0 vs baselines (job 7885093); d4 mu 4962→18084 VmHWM slope 1.9→1.2 GB (replica term gone), wall 67→44 s; CLAIMS 25 |
| bandstructure.dat written by EVERY rank (shared-FS race) | BD.4; htransform.py:1437,1570 | rank-0 writer gate | FIXED 62ba395 — process_index()==0 gate; P=4 srun leg job 7885093: one writer, file exact-0 vs baseline |
| Gamma-label comparison right only by coincidence (norm-0 tie) | BE; htransform.py:1422 | compare canonical label | FIXED 62ba395 — compares the canonical 'Γ' _clean_label emits; argmin fallback now excludes the zero pad rows (the old tie) |
| ~12 stray eager 1-op modules remain | BE, job 7884871 | jit when touched | low |
| P>1 thread-main refusal | BD.3 | prepare_mesh routing | FIXED 24e4dc3; subsumed by e97e8ed (mesh + warm-up now from initialize_communicator_stack) |
| bispinor mode REFUSES on the b600 deck: `lift_to_4spinor` CONCATENATES `psi_S = (alpha/2)(sigma.(k+G)) psi_L` onto `psi_L` without renormalizing, so `||psi_4||^2 = ||psi_L||^2 + ||psi_S||^2 = 1 + O(alpha^2|k+G|^2)`, and `build_fH_R`'s orthonormality cap (1e-6, written for the 2-spinor norm, no bispinor branch) trips at `max|C C^H - I| = 5.368e-05`. It is NOT a span failure — the run's own rank line reads `rank=704` = 16 k x 44 bands, FULL — yet the refusal message lists 'psi-at-centroids cannot span the band window ... fix with MORE CENTROIDS' as cause (1), which misdirects the reader to the centroid set. Same centroid set / window, charge 7885322 vs bispinor 7885966: Gram (704, 9550) -> (704, 19100) (2x, the spinor axis), rank 704 -> 704 (1x), `max|C C^H - I|` 6.910e-14 -> 5.368e-05 = 7.8e8x | jobs 7885966 (both ht legs rc=1), 7885977 (`LORRAX_FH_ORTHO_TOL=1e-4` -> both legs rc=0, 10.7 s = 0.88x the charge wall, 3.88 GiB = 1.68x the charge mem, EQP override proven taken); /scratch2/08271/jackmc/b600_bispinor_p64/RESULTS.md S4.2 | renormalize in `lift_to_4spinor`, or give the gate a bispinor branch that prices the small-component norm; and re-order the refusal message's causes so the rank line is checked first | open — the driver's own conversion bounds the consequence at ~0.48 meV on-grid, ~2000x under the 1e-3 eV significance bound, so the tol override is a bounded workaround |

## bse / exciton bands
| issue | evidence | smallest fix | status |
|---|---|---|---|
| ring/preview path h5py-reads FULL V_q/W0/psi per rank | BD.4; bse_io.py:1453-1470 | use the sharded loader already imported | open |
| psi stacks single-axis mu-sharded (sqrt(P)) | BD.4; bse_ring_comm.py:156, exciton_bands.py:241 | both-axes mu-shard | open |
| eager-FFT debt: bse_feast.ensure_W_R, bse_kpm.run_kpm_dos, bse_pseudopoles x2 (same P0-4 class, ratcheted so it cannot spread) | test_fft_shardmap_context.py allowlist | route through make_w_densifier | open |
| ring transport arm untraceable under current jax (scan-vma check): `_apply_V_ring_only`'s `lax.fori_loop` carry A0 enters unvarying, exits varying `{V:(x,y)}` — TypeError at trace; 10 tests red (dense-reference ring cells, nontda pair, stack-memory, w0/wq resolvent, w_omega_chain); serial+simple matvec arms green, so dense-reference coverage is not blind. This is the old handoff's "bse_ring_comm vma". jax names the fix in the error: `lax.pvary` on the initial carry | bse_ring_comm.py:382 (via :460/:555); jobs 7885150/7885154, /scratch2/08271/jackmc/pytest_p11/junitA2_7885154.xml; repo tests/KNOWN_FAILURES.md | pvary the ring appliers' loop-carry inits (audit every fori/scan carry in the ring path, W arm included) | open |

## bispinor
| issue | evidence | smallest fix | status |
|---|---|---|---|
| Sigma^B = bare transverse exchange only (screened = phase 2) | BISPINOR_DHFB_DESIGN.md:168 | chi^ij + 4x4 Dyson + screened Sigma^B | open (headliner) |
| ~~NO transverse rank gate / basis-adequacy measurement; LU+ridge with no truncation knob~~ INSTRUMENT NOW EXISTS: `transverse_zeta_solve=rank_truncate` (+ `transverse_zeta_rcond` tau, both tiers, any-count distributed) and the per-q `n_keep`/`sig_max`/`sig_min_kept`/`kappa`/`sdrop_hi` log ARE the transverse basis-adequacy measurement. Default stays `ridge` (opt-in): the cut is numerically inert at every certified size | merged e3141e0 (branch `zeta-transverse-ranktrunc`); jobs 7885326/7885327/7885330/7885943/7885947; /scratch2/08271/jackmc/zeta_T_rtrunc/RESULTS.md | — | FIXED (instrument); default-flip re-evaluation deferred to larger mu_T (see conditioning row below) |
| MEASURED CONDITIONING: the transverse Gram is well-conditioned only at small mu_T and degrades steeply — kappa/q 2.14e4..3.18e4 at 136T, 2.97e4..3.79e4 at 143T, **2.23e6..3.35e6 at 275T**, i.e. kappa ~ mu_T^6.6..6.9, crossing 1e6 BETWEEN 143T and 275T. Consequence: at tau<=1e-8 nothing is dropped at any tested size, but at 275T/tau=1e-6 the cut drops 3..6 of 275 modes — worth only 2.4e-6 eV of eqp, so measurable in n_keep and inert in the observable. The original "TRS near-null directions pollute the transverse solve" hypothesis is CONFIRMED as a trend but is NOT yet consequential at certified sizes. Extrapolating the exponent to the 1600T production target gives kappa ~ 5e11, i.e. the LU+ridge floor (1e-12*tr/n) would then be inverting through genuinely near-null directions | jobs 7885327 (136T/143T ladders), 7885943 (275T ladder, true full-set kappa from the tau<=1e-8 legs), 7885947 (bi16 275T); /scratch2/08271/jackmc/zeta_T_rtrunc/RESULTS.md | re-measure kappa when mu_T grows; flip the default to rank_truncate only once the cut moves the observable | MEASURED AT PRODUCTION (job 7885987, mu_T=1591): kappa 2.30e11..2.82e11, four-size law kappa = 2.5e-10*mu_T^6.566 (fit within 7% at every size) — the trend HELD, the 5e11 extrapolation was 1.8-2.2x high; the observable is still inert (three rows below). watch continues |
| TT tiles: q=Γ G=0 slot stored as 0 with NO mini-BZ head substitute (CC gets one; CT/TC exactly zero by Coulomb gauge, SOC-independent). Slab kernel v ~ (8πz_c/V)/K at K→0 (integrable, ~1/√N_k class); current structure factor ⟨mk\|α^i\|nk⟩ does not vanish. Measured bi4 (4-leg restart-doctored A/AR/B/C, AR-vs-A exact-0): missing head = eqp/Σ_X max 0.209 meV (mean 0.037), −0.076 eV of the −1.553 eV Σ^B trace (4.9%); whole q=Γ TT term 0.347 meV max (7.8% of Σ^B). ⟨v·t^{ij}⟩_mBZ = (0.4993, 0.5007, 1.0000)·vc0 diag, off-diag ≈ 0 | BISPINOR_DHFB_DESIGN.md §11 (merged 4cc65ac); job 7885325; /scratch2/08271/jackmc/bispinor_gamma_check/ (doctor_report.7885325.json, gamma_check_analysis.7885325.txt, run_{A,AR,B,C}) | mini-BZ ⟨v t^{ij}⟩ substitution at the q=Γ G=0 slot in v_q_bispinor._make_per_q_v_builder_for_tile (single file, deterministic v-table change); changes baselines → owner re-pin | open; SUP (default change) |
| TT Lorentz-mix (IBZ cascade on) holds ~27 tile-equivalents concurrently: tt_buffer 6 + 3 Hermitian conj + 9-stack V_in + 9 V_out, sharded /P — 17.7 GB total at 1600T/4x4 (276 MB/rank @P=64; 4.4 GB/rank @P=4) | v_q_bispinor.py:261,373-401; common/symmetry_maps.py unfold_v_q_bispinor_lorentz (jnp.stack); memory audit job 7885325 | mix per output tile (accumulate over (α,β)) or del tt_buffer refs pre-stack — peak → ~11 tiles | open (low) |
| BispinorVqReader._zero_tile materializes the full (nq, μ_C, μ_T) eager jnp.zeros on ONE device per process (~2 GB at 4800C+1600T) before the sharding constraint; dormant — no live CT-tile caller today (phase 2 will call it) | v_q_bispinor.py:483-491; probe job 7885325 (bare jnp.zeros = SingleDeviceSharding; constraint redistributes after) | build via jax.jit(lambda: zeros, out_shardings=...) like v_q_g_flat's accumulators | open (latent) |
| σ^B transverse ψ bundle: 4 copies single-axis μ-shard (√P class, same as the bse/kmeans rows) + 2 transient γ̃-applied copies per (i,j) pair — 123 MB/rank/copy at 1600T/b600/P=64, bundle 492 MB/rank persistent | gw/wavefunction_bundle.py:96-99; gw/sigma_x_bispinor.py:62-100; memory audit 2026-08-01 | both-axes μ-shard (shared fix with the bse psi-stacks row) | open (class) |
| σ^B cannot reach the FUSED flat-k `gw_conv` FFI: its only construction site is `ppm_tau_kernel.py:256` (charge Σ_c τ kernel), and `sigma_x_bispinor` builds `_make_cohsex_kernels`, whose `_convolve` uses the DECOMPOSED 3-FFT chain and materializes the R-space G tile. σ^B therefore holds 3 concurrent `(nk, ns², μ_T²)/P` slots instead of 1: 507 MiB/rank at 1604T/P=64 vs 169 MiB fused (+338 MiB/rank), and 7.92 vs 2.64 GiB/rank at P=4 (+5.3 GiB/rank) — held 9x sequentially. The same fix removes ~2.75 GiB/rank from charge Σ_X at 4800c/P=64 | gw/sigma_x_bispinor.py:154-156, gw/cohsex_sigma.py:113-126 vs gw/ppm_tau_kernel.py:249-258; common/fft_helpers.py:316; parity audit 2026-08-02 | route `_make_cohsex_kernels` through `make_flat_k_gw_conv` when the fused gate is on — the same handler the τ kernel already uses | open |
| σ^B evaluates 9 (i,j) tiles where only 6 are unique, and rebuilds ψ 9 times where 3 suffice: `BispinorVqReader.get_tile` serves (2,1)/(3,1)/(3,2) by RE-READING the companion dataset and `conj(swapaxes(...))` (3 redundant sharded reads of 10.6 MiB/rank + 3 x↔y all-to-alls + a second jit signature for `sigma_sx_k`), and `_wfns_with_lorentz_vertices` applies γ̃^i to psi_xn (depends on i only) and γ̃^j to psi_yr (j only) INSIDE the inner loop — 9 materializations each instead of 3, at 122 MiB/rank per copy = ~732 MiB/rank of avoidable traffic at 1604T/600b/P=64 | gw/sigma_x_bispinor.py:62-100, :178-194; gw/v_q_bispinor.py:532-542; parity audit 2026-08-02 | hoist the 3 psi_xn and 3 psi_yr variants out of the loop; cache `get_tile` on the 6 unique tiles | open |
| σ^B forces 18 host syncs per call and has no AOT precompile: `contrib.block_until_ready()` plus an UNCONDITIONAL `float(jnp.einsum('kmm->', contrib))` trace D2H inside the (i,j) loop (`verbose`/process_index gate only the print, not the sync), so tile n+1 cannot dispatch while tile n runs; and `sigma_sx` compiles at first dispatch — twice, because the Hermitian-transposed operand is a second signature — inside the stage. Contrast the charge τ path's lag-2 persistent async D2H deque (8a9a21c) and `precompile_sigma` | gw/sigma_x_bispinor.py:182-194; gw/ppm_accumulators.py:203-318, gw/ppm_tau_kernel.py:478; parity audit 2026-08-02 | gate the trace on `verbose and process_index()==0`, keep only the final `block_until_ready`, add an AOT precompile for `sigma_sx` at both signatures | open |
| σ^B, the bispinor V_q build and the Lorentz mix have ZERO timing sections: `grep -c timing.section` = 0 in gw/sigma_x_bispinor.py, gw/cohsex_sigma.py and gw/v_q_bispinor.py, against 7 in ppm_sigma.py and 10 in ppm_tau_kernel.py. At bi4 the whole σ^B assembly is buried in `gw_jax.sigma` self-time (1.475 s) with no breakdown, so the stage cannot be measured at production scale without a code change — this audit had to project it | job 7885325 timing table (/scratch2/08271/jackmc/bispinor_gamma_check/run_A/gw_dev.7885325.log); parity audit 2026-08-02 | wrap the (i,j) loop, the three `_convolve` legs and the 7-tile V_q loop in `timing.section` | open |
| σ^B projects with the replicating `wavefunction_bundle.project` (two plain einsums; the first all-reduces the 'x'-sharded μ_X so the intermediate is x-replicated, the second all-reduces 'y' to a fully replicated (nk,nb,nb)) instead of `contract_bands_block_reshard` — no psum_scatter, no vendor GEMM FFI, 2 all-reduces per tile x 9 tiles. Cheap at nb_σ=44 (8.9 MiB/rank intermediate, ~0.5 s) but scales as nb_σ: at nb_σ=600 the intermediate is 122 MiB/rank and the flops 13.6x larger | gw/wavefunction_bundle.py:372-376 vs common/contract_bands.py:219 (production call sites: ppm_tau_kernel.py:198, bse_ring_comm.py:501/763 only); parity audit 2026-08-02 | swap `project` for `contract_bands_block_reshard` inside `_make_cohsex_kernels` | open (latent — binds at large nb_σ) |
| RIDGE IS INERT, NOT PROTECTIVE, at production mu_T: the transverse LU ridge `delta = 1e-12*\|tr(C_log)\|/n_log` (`isdf/core.py:2212` constant; :2234 local hoisted LU = the route production takes, :2462 scalapack, :3566/:3686 fused fallbacks) obeys `\|tr\| <= sum\|lam\| <= n*sig_max`, so `delta <= 1e-12*sig_max <= 1.083e-17`, against a MEASURED `sig_min = 3.82e-17..4.29e-17` at mu_T=1591. ZERO modes per q per channel sit at or below the ridge scale (hard bound; worst-case margin 3.53x). The ridge neither regularizes nor perturbs — the LU inverts the true kappa=2.6e11 spectrum unregularized (kappa*eps = 6.3e-5 relative on zeta_T). `_charge_factor_math`'s docstring claim that the ridge family is "lifted only to the 1e-12*tr/n ridge floor, kappa~1e12" is REFUTED as a mechanism; its conclusion (rank truncation is the instrument worth having) survives | job 7885987 legs rt_1e-12 + ridge; /scratch2/08271/jackmc/zeta_T_prodkappa/RESULTS.md sec.3 | correct the docstring; add a rank-0 announce of delta and \|tr\|/n_log on the ridge path so a default run can see its own ridge scale | open — documentation/observability, not a numerical defect |
| A POSITIVE DIAGONAL RIDGE ON A HERMITIAN INDEFINITE MATRIX IS NOT A REGULARIZER: `C + delta*I` shifts EVERY eigenvalue by +delta, so it lifts positive near-zero modes away from zero and pushes negative ones THROUGH zero — a mode at lam = -delta is annihilated exactly. The transverse CCT is indefinite by construction (both signs physical; it is why rank_truncate cuts on \|lam\|). Harmless today ONLY because delta sits below the whole spectrum (row above), not harmless by construction, and actively harmful rather than merely useless at the size where it starts to bite. By the measured law sig_min/sig_max = 3.53e-12*(mu_T/1591)^-6.57, that is mu_T ~ 1900 if \|tr\|/n_log saturates its bound, later otherwise | job 7885987 spectrum (sig_min/sig_max at 136/143/275/1591T); isdf/core.py:2234 | resolve-time REFUSE, or auto-promote to rank_truncate, above a MEASURED kappa > 1e12 — kappa is the invariant, mu_T only its proxy | open — default-policy call, but the threshold is now measured |
| NO CONDITIONING INSTRUMENT ON THE DEFAULT PATH: the per-q `n_keep`/`sig_max`/`sig_min_kept`/`kappa`/`sdrop_hi` log (`LORRAX_ZETA_RANK_LOG`, on by default) is emitted ONLY inside the rank_truncate branches (`isdf/core.py` `_charge_factor_math` mode `transverse_rank_truncate`; `_factor_c_q_distributed_rank_truncate`) — exactly the branches a ridge-default run never enters. A production bispinor run therefore cannot see its own transverse conditioning, and measuring it costs a full extra GW leg (11 min on 32 nodes, which is what job 7885987 had to spend) | job 7885987 needed a dedicated rank_truncate leg to read a quantity whose trace the ridge leg already forms | cheap: the hoisted LU already computes \|tr(C_log)\|; announce it (rank 0, once per channel) with n_log and the resulting delta. Full: a `transverse_zeta_probe` running the eigh on one q only | open |

## cross-cutting
| issue | evidence | smallest fix | status |
|---|---|---|---|
| INSTRUMENT TRAP: `sacct` MaxRSS silently reports the WRAPPER, not the driver, whenever a harness runs python as a CHILD of the container shell instead of `exec`ing it as the srun task. Measured job 7885315: sacct MaxRSS 15800K / 26928K / 18336K for three steps whose real per-rank peaks (/proc VmHWM) were 10.61 / 2.10 / 2.15 GiB — a **~700x undersample**, and it looks like a plausible small number rather than an error. The certified `gw_dev.sbatch` template `exec`s python, which is the only reason historical sacct figures (b300 4.63 GiB, job 7885024) are real | jobs 7885315 vs 7885024; /scratch2/08271/jackmc/b600_p64/harness/inner_common.sh | any harness that does not `exec` python must sample /proc/<pid>/status VmHWM itself and must not quote sacct MaxRSS; note it next to the AY brief-peak caveat in the scorecard | open — documentation/practice, no code change |
| `_zeta_reuse_ok`'s legacy-missing-key exception was expressed as a value-difference list over the key UNION, so a key ADDED with a value equal to its legacy default landed in NEITHER list: the difference list came out empty while the JSON strings still differed, and an empty list fell through to the generic `fit under DIFFERENT inputs` refit with an EMPTY `Changed:` detail. Every pre-2026-08-04 charge zeta would have been refit ONCE for nothing (b300/b600 run dirs included) -- the exact outcome the exception exists to prevent. Latent before 2026-08-04 because the only legacy keys then had non-None defaults ('replicated'/'ridge'), which always produced a real value difference | job 7888522 (`tests/test_zeta_provenance_matrix.py::test_pre_20260804_stamp_reused_by_charge_run_refit_by_bispinor` red); gw_init.py `_zeta_reuse_ok` | key the exception on the ADDED/DROPPED key sets, not on differing values; refuse the exception outright when the stamp carries a key this build does not write (it came from a NEWER LORRAX) | FIXED 82cefa2 -- three regression tests added (added-key-equal-to-default reuses; newer-stamp refits; a real input change alongside a legacy gap still refits) |
| psp.valence_density_from_kpoint: 17 eager modules, one jit away | BE | jit it (3 call paths — test all) | FIXED d992634 — row was STALE. `_valence_density_kernel` is `@partial(jax.jit, static_argnames=("nocc",))` with weight/cell_volume/spin_degeneracy as TRACED operands, so the per-k sweep reuses ONE compiled module per box shape instead of ~17 eager 1-op modules per call. All three call paths route through it (get_DFT_mtxels.py:335 chunked CLI, gw/kin_ion_io.py:332 distributed sweep, common/density_symmetry_check.py:652 checker) — the docstring names it the single source of truth for the density quadrature |
| mu-convergence non-monotone at 512b (c4951 -> c6947: -240/-183 meV) | OWNER_DECISIONS | convergence-sweep driver + adequacy re-measure | open; SUP for the verdict |
| six mos2_4x4_test sbatch harnesses still carry the compile-cache opt-out | BE | delete ISDF_JAX_CACHE_DIR="" lines | NOT-A-DEFECT — row was STALE and its prescribed fix was WRONG. Only 4 harnesses still mention the variable, in two distinct roles: (a) gw_ht_b300.sbatch:119 and post_b300.sbatch:65 already run cache-ON and carry a comment recording exactly that (evidence jobs 7884869/7884871: warm = 0 compiles on the dft leg, 2 on qp, bandstructure.dat byte-identical to cache-off); (b) ht_compile_probe.sbatch:40 and ht_compile_verify.sbatch:42-43 set it ONLY under `[ "$CACHE" = off ]` — a deliberate A/B lever, since these harnesses ARE the instrument that measures cache behavior. Deleting those lines would destroy the instrument. No production harness opts out |
| QE->pw2bgw leg uncertified on Frontera | ASSERTIONS.md | one certified NSCF example (7884642 is the candidate) | open |
| head-wing-fix rescue patches exist ONLY on Perlmutter checkout | _archive/rescue_2026-07-22 | recover after outage | SUP (data risk) |
| SlabIO h5py_allgather fallback REPLICATES every read: BispinorVqReader.get_tile / gw_init CC readback land the full (nq, μ²) per rank + host copy — CC tile @4800c = 5.9 GB ×2 PER RANK (invisible at ≤275T where a four-tile block is ~20 MB); production slab_io=auto→FFI reads sharded /P and is safe | file_io/slab_io.py:256 (docstring states it); _slab_io_allgather.py; memory audit job 7885325 | byte-threshold gate: refuse (or loudly demote) allgather reads above a size cap | open |
| A REFUSAL INSIDE A COLLECTIVE MUST FIRE ON EVERY RANK OR NONE — the phdf5 write bounds test was still taken on THIS rank's advanced offset after d935ce7, so it reached a verdict only on ranks whose selection is NON-empty. When the caller's logical slab overruns the dataset extent AND some ranks are wholly padding, the two populations disagree: the ranks that do not refuse enter the collective `H5Dwrite` alone. Captured verbatim at P=4 (per-rank stderr): `rank=2 extent=[2,5,8] offset=[0,5,0] count=[2,1,8] oob=1 empty=0` while ranks 0/1 are `oob=0 empty=0` and rank 3 is `oob=0 empty=1` — ONE of four refuses, the job hangs 306 s and dies at the JAX shutdown-barrier deadline with no HDF5 error and no traceback. At P=16, rank 8 alone. The READ path was worse: `read_ffi.cc` had NO bounds test at all, which made `H5Sselect_hyperslab` the de-facto one — same asymmetry, and the P=4 read case produced ZERO output on EVERY rank before the external timeout killed it at 420 s | jobs 7888470 (arm=base, the hangs), 7888525 (arm=fix); /scratch2/08271/jackmc/slabio_audit/RESULTS.md S1 | FIXED 8050e34: take the bounds test on the LOGICAL slab, `offset_base + valid_shape`, in BOTH write_ffi.cc and read_ffi.cc. Both are replicated control vectors, and max over ranks of `offset[d] + file_count[d]` is exactly `offset_base[d] + valid_shape[d]`, so the predicate is identical — only WHO evaluates it changes. A globally empty request (`valid_shape[d]==0`) is still never bounds-tested (d935ce7 doctrine). Certified: p4_oobw 306 s hang -> 7 s with 4/4 ranks refusing by name; p16_oobw hang -> 6 s with 16/16; p4_oobr 420 s silent timeout -> 6 s with 4/4. Accept path unchanged: 23 round-trip checks exact-0 at P=4 and P=16 on BOTH arms. A/B is one .so diff (build_host_PADFIX vs build_host_SLABIO, exported-symbol diff EMPTY, 475 both) | FIXED |
| `read_ffi.cc` had no dataset-rank check where `write_ffi.cc` has always had one. Latent until the audit added an extent read: `H5Sget_simple_extent_dims` fills file_rank entries into a request-rank-sized vector, so an N-D request against an (N+k)-D dataset overruns it | code reading, 2026-08-04 audit | FIXED 8050e34 — `H5Sget_simple_extent_ndims` check before the extent read; rank-invariant, so it refuses on every rank or none | FIXED |
| `_FfiBackend.read_slab` did not drain the write dispatcher. `write_slab` only ENQUEUES; the drain lived in `_ds_id`, which runs only on FIRST sight of a dataset name, and `_introspect_dataset` (serial h5py on the same path) ran before even that. Three hazards: reading pre-write bytes with no error anywhere; `ctx->pinned_buf` memset under an in-flight H5Dwrite reading from it (CUDA build); two threads inside HDF5/MPI-IO on one file handle — the hazard `create_dataset` already drains for — plus a collective-order mismatch if rank A reads-then-writes while rank B writes-then-reads. MEASURED: the window IS reachable — `dispatcher.pending = 1` at read_slab entry on every rank with 12 queued 1 MB/rank slabs, and on the base arm that write completed CONCURRENTLY with the read (`draining 0 pending writes` at close). It produced no wrong data at this scale; at production scale the window is one H5Dwrite (~11 s) wide, not one microsecond | jobs 7888525 (fix), 7888528 (base control); /scratch2/08271/jackmc/slabio_audit/RESULTS.md S4 | FIXED 8050e34 — one unconditional `_drain_pending()` at the top of read_slab; free when the queue is empty. PLAUSIBLE, not CONFIRMED: precondition measured, corruption not observed | FIXED |
| Collective-call counts gated on per-rank Python state in both phdf5 backends' `close()`: `if self._deferred_attrs:` guarded a `_barrier` (FFI) and a loop of TWO collectives per attr — `comm.bcast` + collective `create_dataset` (host). Every `write_attr` call site in the tree is SPMD today (tagged_arrays, v_q_bispinor, sigma_output — all conditions rank-independent), so the counts match; that is a property of the callers, not of the method, and it is not checkable there | code reading, 2026-08-04 audit | FIXED 8050e34 — FFI barrier is now unconditional; the host backend agrees the deferred-attr NAME LIST across ranks with one unconditional allgather and refuses by name on a divergence rather than hanging | FIXED |
| `ffi.io.open_file` caches contexts by path and IGNORED the mode, so a `mode='w'` caller on an already-open path gets the cached context back — and `_replace_inode_for_write` has already unlinked the target on rank 0, so every subsequent write lands in an ORPHANED inode and the run finishes rc=0 with nothing at the path. A `mode='r'` context handed to a `'w'` caller loses the write the same way | code reading, 2026-08-04 audit | FIXED 8050e34 — the mode is recorded with the handle and a mismatch refuses by name. Deliberately NOT measured: exercising it means two SlabIO objects sharing one `PhdfCtx*`, and the first `close_file` frees it under the second — a double-free, not an experiment | FIXED |
| `_MpiHostBackend.read_slab(as_numpy=True)` returned THIS RANK'S BLOCK for a sharded request where `_AllgatherBackend` returns the full slab — 1/P of the data typed as the whole thing. Unreachable through `SlabIO.read_slab` (which does not forward `as_numpy` on the phdf5 paths and converts the returned array itself), reachable by using the backend directly, which the tests do | code reading, 2026-08-04 audit | FIXED 8050e34 — refuses a sharded request, naming the spec | FIXED |
| `phdf5 ensure_dataset` does NOT verify an existing dataset's shape or dtype (`ffi/cpp/phdf5/context.cc:506`): `H5Dopen` succeeds, the hid_t is returned, and the write clips against whatever extent that dataset happens to have. In `mode='a'` a rerun at a different mu silently writes into the old geometry and leaves the rest stale | code reading, 2026-08-04 audit; ruled on by decisions.md 2026-08-04 | verify shape+dtype and refuse (rank-invariant, so it refuses everywhere or nowhere) | FIXED 98afe58 — `check_existing_geometry` compares ndim, dims and `H5Tequal` on BOTH the cached-hid and freshly-opened paths, and refuses naming both shapes. It DOES newly refuse a `mode='a'` rerun at a different mu; the ruling says those were writing a prefix into the old geometry and were never correct. It is also what makes SlabIO's Python-side record of the dataset shape authoritative, which is where the derived `valid_shape` comes from. Certified jobs 7888650 (P=4) / 7888651 (P=16), .so `build_host_IMPLICITPAD` sha 51c69680d078bf85 |
| `create_dataset` on an ALREADY-EXISTING dataset diverges across the three backends: `_AllgatherBackend` does `del self._file[name]` then recreates (contents reset), `_MpiHostBackend` returns early ('respect the existing dataset'), and the C++ `ensure_dataset` reuses it. A caller that creates then writes only part of the extent gets different bytes in the untouched region depending on which backend the capability router picked | job 7888470/7888525 rt matrix (which always writes the full extent, so it does NOT cover this); code reading; ruled on by decisions.md 2026-08-04 | unify on reuse-if-identical / refuse-otherwise; the allgather delete-and-recreate is the outlier and is data loss | FIXED 98afe58 — all three now reuse on identical shape+dtype and refuse otherwise, naming both shapes, via one shared `_DatasetGeometry._refuse_geometry_change`. The allgather verdict is computed on rank 0 and BROADCAST (`multihost_utils.broadcast_one_to_all`), because only rank 0 holds a handle and a rank-0-only raise would strand the peers in the create barrier. Unit gate `test_create_dataset_refuses_a_geometry_change` (39 passed, job 7888651) |
| `AsyncDispatcher._raise_if_error` CLEARS the stashed worker exception as it raises, and `_FfiBackend.close()` calls `_drain_pending()` BEFORE `_close_file`. A worker exception on one rank therefore aborts that rank's `close()` before the collective `H5Fclose`, leaving the file handle open and `_closed` unset while its peers close | code reading, 2026-08-04 audit; ruled on by decisions.md 2026-08-04 ('no rank may skip a collective because of its own error') | record the error, complete the teardown, then raise | FIXED 98afe58 — `_FfiBackend.close()` catches around both the drain and the dispatcher close, announces on the affected rank, completes `H5Fclose` and the close barrier, and re-raises at the end; the rank-0 deferred-attr h5py write is skipped when the worker failed (writing metadata that claims completeness over a failed write is worse than not writing it). `AsyncDispatcher.close()` joins the worker in a `finally`. `_MpiHostBackend.close()` likewise closes the file in a `finally` before raising its deferred-attr divergence. NOT separately measured: exercising it means injecting a writer-thread exception, which is a different instrument |
| `ctx->pinned_buf` is ONE buffer shared by the SYNCHRONOUS `ReadImpl` (runs on the XLA thread, starts with ensure_pinned + memset, and ensure_pinned may free and realloc) and the writer thread (async write on the CUDA build; `read_kchunk_union` on both). The SlabIO exposure is closed by the read-side drain; a direct `ffi.io` consumer mixing the sync reader with the kchunk-union reader on ONE handle still races | code reading, 2026-08-04 audit; `ffi/cpp/phdf5/{read_ffi.cc,ctx.h}` | route the sync read through the ctx writer queue like the other two handlers, or give the sync path its own buffer | open (C++ redesign) — DOCUMENTED PRECISELY 98afe58: `ctx.h` now names the two producers, states that serialisation among the writer-thread tasks is by construction (one thread, one FIFO, each task syncs the previous H2D) and that NOTHING orders `ReadImpl` against them, states that `_FfiBackend.read_slab`'s unconditional drain closes the window for every SlabIO caller, and forbids adding a second sync entry point on this buffer without doing the redesign first |
| `H5D_FILL_TIME_NEVER` + `H5D_ALLOC_TIME_EARLY` (the phdf5 dcpl) make 'a dataset created and never written reads back zero' a FILESYSTEM property, not an HDF5 guarantee — and `tagged_arrays.write_restart_state_to_h5(init_W0=True)` advertises exactly that as an 'all-zeros W0_qmunu placeholder'. MEASURED TRUE on a fresh Lustre inode (0 nonzero of 80), which is what production always gets because `mode='w'` unlinks the target. It would not hold for a dataset allocated inside a reused region | job 7888470/7888525 rt case, fill-never check | none needed today; state the dependency where the placeholder is documented | open — documentation |
| SlabIO PADDING IS NOW IMPLICIT — the caller states logical shapes only. `valid_shape` used to be computed at every call site as a restatement of the dataset's own extent, and every SlabIO defect found on 2026-08-02/04 was that arithmetic going wrong at one of them. It is now DERIVED, `min(operand, dataset - offset)` per dim, in one place (`_slab_io_ffi._derive_valid_shape`), from a REPLICATED record of the dataset geometry (`_DatasetGeometry`) that `ensure_dataset`'s new shape/dtype refusal keeps authoritative. `valid_shape` survives only as the ragged-chunk override, and an override that overruns the dataset REFUSES (in Python now, on every rank, before any collective is entered — the C++ tests in write_ffi.cc/read_ffi.cc remain the backstop for direct `ffi.io` users). `global_shape` is create-only and refuses when it contradicts an existing dataset | decisions.md 2026-08-04; jobs 7888650 (P=4) / 7888651 (P=16): all 8 padded-rank cases + 4 probe cases rc=0, 3x3 writer x reader matrix 0.000e+00 at both P, 39 unit tests | — | FIXED 98afe58 + 94a0a75 + ca9bad5b. BYTE-IDENTITY CERTIFIED: the same padded buffer written with the old explicit spelling and with bare `write_slab(name, A)` gives sha256 `c12e54ab4a217c1a` at P=4 (mu=5) and `33af697ff8fc5d6c` at P=16 (mu=17) — identical in each pair — and the two read spellings return byte-identical blocks on every rank. Call-site sweep: 15 `valid_shape=` and 10 `global_shape=` arguments deleted, plus the whole `valid_mu` parameter from ZetaLoader and its one consumer |
| MESH DIVISIBILITY IS NOT A SlabIO CONTRACT TERM — it is JAX's constraint on the (shape, spec) pair the caller chose, and SlabIO cannot pad its way around it. MEASURED (jax 0.9.1, gate `PADRANK_CASE=nodiv`): `jax.device_put` of a shape whose sharded dim does not divide its mesh-axis product raises `IndivisibleError`, so such an array cannot be constructed at all. Consequences: SlabIO is never HANDED a non-divisibly-sharded operand on the write path (there is nothing to pad), and it could not RETURN one on the read path either — reading a rounded-up extent and trimming, which was implemented and then deleted, only replaces SlabIO's message with JAX's at the same refusal. This also settles the audit's item 5: `_MpiHostBackend`'s absent divisibility check is NOT a capability difference, because the case it would accept cannot be expressed | jobs 7888650 / 7888651, `nodiv` case, 4 of 4 and 16 of 16 ranks refusing by name | — | RESOLVED 94a0a75 — `_validate_block_divisible` is kept as an early, numbered restatement of JAX's own constraint that names the rounded-up extent to ask for instead |
| INSTRUMENT TRAP (found while implementing the above, job 7888644 probe/raw, rc=1 on 4/4): opening a file with SERIAL h5py while the same process holds it open for collective MPI-IO WRITING fails with `OSError: file signature not found` — the superblock is not durable yet. `_FfiBackend._introspect_dataset` is therefore safe only for a dataset this handle did NOT create; making it unconditional broke read-after-write-on-one-handle. Any future code that wants dataset metadata on a write-open phdf5 handle must take it from the handle's own record, not from the file | job 7888644 | — | FIXED 94a0a75 — `_dataset_geom` answers from `_DatasetGeometry` first and only introspects for a dataset the handle did not create |
| INSTRUMENT TRAP: `tests/test_restart_pad_roundtrip.py` FAILS when it shares a pytest process with `tests/test_sanity_gates_jax.py` or `tests/test_contract_bands.py`, which set `XLA_FLAGS=--xla_force_host_platform_device_count=4` at IMPORT time. `padded_mu_extent(6, 4)` with `LORRAX_EXTRA_MU_PAD=2` is 12, not the 8 the test expects at one device, so the restart round-trip compares (3,12,12) against (3,8,8). Pre-existing and revision-independent: A/B on the SAME combined command gives 2 failed on BOTH `fix/slab-io-audit` @ ca9bad5b (136 passed) and its base dccb02e (130 passed), and the file passes ALONE on both | job 7888660 (four legs: combined FIX / combined BASE / alone FIX / alone BASE) | either give the device-count-forcing files their own pytest invocation, or make `test_restart_pad_roundtrip` compute its expectation from `jax.device_count()` instead of assuming 1 | open — harness |
| padded_mu_extent rounds μ to multiples of P (p_x·p_y): at P ≳ μ the pad inflates every tile/ψ quadratically — μ_T=1600 @P=4096 → padded 4096 (6.6× tile bytes; V tile 1.05 GB/rank vs 0.16 ideal) — blocks the thousands-of-procs target for the transverse set | runtime/padding.py; v_q_bispinor.py:498-517; v_q_g_flat.py:378-385; memory audit 2026-08-01 | pad per-axis (multiple of p_x on left μ, p_y on right μ) after a consumer divisibility audit | open (scaling) |
| phdf5 collective WRITE refuses a rank whose local slice is ENTIRELY padding: the bounds test `offset[d] + file_count[d] > extent[d]` (`ffi/cpp/phdf5/write_ffi.cc:233-236`) runs BEFORE and independently of the `empty_selection` / `H5Sselect_none` branch, and `offset[d]` has already been advanced by `local_start = rank_coord*mem_dims[d]`. At mu_T=1604 / P=64 (padded 1664, loc=26) ranks 62-63 own pure padding, so offset 1638 > extent 1604 (files store the LOGICAL shape) trips `kInternal` and kills all 64 ranks at the collective close of `tmp/zeta_q_mu1.h5` — no traceback, rc=1 everywhere, a 670,564-byte file where 504 MB was due. Reachable ONLY when pad >= loc, i.e. mu more than one rank-slice below its padded extent: the charge channel (4775/4800, pad 25 < loc 75) and every P<=16 bispinor run are structurally immune, which is why this survived to P=64. NOT memory — budget 40 -> 20 GB halved peak VmHWM 30.19 -> 16.12 GiB and reproduced the failure byte-for-byte. `read_ffi.cc:140-149` has the equivalent guard and is CORRECT | jobs 7885953 (both failed legs; `run_gw_bi{,_m20}/tmp/zeta_q_mu1.h5` kept as evidence), 7885966 (controlled fix: mu_T 1604 -> 1591, pad 9 < loc 25, same code and budget, rc=0 with all three transverse files at their full 504,150,144 bytes); /scratch2/08271/jackmc/b600_bispinor_p64/RESULTS.md S4.1 | FIXED d935ce7: decide `empty_selection` FIRST and let it veto the bounds test (only a non-empty selection can run off the end). The earlier CAVEAT is now RESOLVED — the missing `oob=` fprintf was not evidence against the attribution, it was the harness losing stderr at teardown (the same effect `runtime.install_failfast_excepthook` documents); per-rank stderr capture recovered it verbatim: `rank=3 extent=[2,5,8] offset=[0,6,0] count=[2,0,8] oob=1 empty=1`. One-line control: two libs from the SAME rev differing only by this change (build_host_PADBASE 45f86e20 vs build_host_PADFIX 13a0b667, identical exported symbols) — P=4 mu=5 and P=16 mu=33 go rc=1 -> rc=0 PASS, three controls unchanged (jobs 7886446/7886450/7886451). END-TO-END: job 7886458 ran the exact failure shape mu_T=1604/P=64 to rc=0 in 648 s with all three transverse files at 508,235,264 bytes, and the mu_T=1591 control leg is EXACT-0 vs certified 7885966 on eqp0/eqp1/eqp_g0w0/sigma_diag (only the timestamp header differs). Gate: tests/multi_device/phdf5_padded_rank_write.py | FIXED |
