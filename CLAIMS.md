# CLAIMS.md — measurement and decision ledger

One line per claim. Append; never delete. When a claim is overturned, set
its verdict to REFUTED or add the superseding row's number to its last
column — do not edit the statement. A claim not in this ledger, or without
a jobid/artifact, is a hypothesis: test it, do not cite it. Deep context
for any row: grep `/scratch2/08271/jackmc/lorrax_setup/docs/SPEEDUP_SCORECARD.md`
for the jobid.

Verdicts: GREEN (measured, holds) | REFUTED | RULE (binding practice,
evidence-backed) | FIXED (defect closed) | OPEN.

| # | Date | Claim | Evidence | Verdict | Superseded by |
|---|---|---|---|---|---|
| 1 | 2026-07-31 | Full 4x4 GW pipeline on the vendored template: rc=0 in 2:37; eqp parity exact-0; sigma_mnk.h5 parity 1.1e-13 eV vs pinned baseline | jobs 7884609, 7884612 | GREEN | |
| 2 | 2026-07-31 | W-densifier compiles with 0 gather-class collectives in both output modes; numeric parity 0.000e+00 | job 7884609 probe step; repo `tools/probe_w_densifier_hlo.py` | GREEN | |
| 3 | 2026-07-28 | jax CPU collectives `mpi` green at AQ 4962c/P=64, both distributed tiers forced; Dyson residual <= 7.5e-15 | jobs 7877754 (warm, 554 s), 7877789 (cold, 514 s) | GREEN — production transport | |
| 4 | 2026-07-28 | gloo/ib0 fails reproducibly at P=64 distributed tiers (ReduceScatter tcp read timeout, gloo `buffer.cc:72`); also observed silently corrupting ~5% of reduce-scatters | jobs 7877753 (warm, rc=1 @211 s) + 7877761 (cold, rc=1 @227 s); scorecard §AY | REFUTED as transport for P>=64 | |
| 5 | 2026-07-28 | Collective-table gates are valid only cache-cold: cache-hit modules never re-dump HLO, so warm tables under-report | scorecard §AY | RULE | |
| 6 | 2026-07-28 | Flat-k MKL FFT (DFTI API) FFI: sigma.exec 272 -> 71.9 s (3.78x) at AQ 4962c/P=64; tau FFT trio 38.5x; HLO transposes 6 -> 0; unit gate 1e-16; h5 parity 2.5e-14 eV | jobs 7878708/7878719/7878727/7878745; repo commit 5918cf6 | GREEN — certified opt-in | |
| 7 | 2026-07-28 | Global k-leading relabel ("route b") would help the FFT layout | HLO probe job 7879370 (repo 068286c): batched minor-most FFTs move 0.00 MB; relabel pushes BSE into the costly layout | REFUTED | 6 |
| 8 | 2026-07-27 | `warm_mesh_cliques(mesh)` must run (x + y + world cliques) before the first jitted mpi collective; without it BSE TDA Lanczos dies with thread-main refusals (32 refusals at P=16) | failure job 7879458; discriminating controls 7881053; gate 7881216; enforcement `src/common/collectives.py::warm_mesh_cliques` | RULE | |
| 9 | 2026-07-28 | `check_hermitian` eagerly transposed the sharded (mu,mu) W tile, gathering 399 MB per operand per call; fixed with `with_sharding_constraint` (one fused jit was NOT enough) | repo test `test_check_hermitian_sharded_no_full_gather`; scorecard §AY | FIXED | |
| 10 | 2026-07-28 | Omega-cube fix (consume tiles where reduce-scatter left them, existing 2D mesh): gathers eliminated, byte-identical results | jobs 7878707, 7878722 | GREEN | |
| 11 | 2026-07-28 | Gram-build column-blocked fix removes the 98 GB single-node centroid Gram wall; byte-identical at c2475; mu~7k centroids fit 192 GB nodes | gate job 7878488 | GREEN | |
| 12 | 2026-07-28 | Memory model needs no recalibration: planner-vs-VmHWM 0.97-1.06 across ladder rungs 1-4 (walls 514/434/495/795/1217 s); memory wall extrapolates to mu~26k at P=64. `sacct` MaxRSS undersamples peaks; `/proc` VmHWM is authoritative | ladder record, scorecard §AY + `lorrax_setup/wk_REL/` ladder notes | GREEN | |
| 13 | 2026-07-28 | Intel MPI provider is `mlx` in-container; `FI_PROVIDER=tcp` only as the rtx escape hatch | `config/frontera/mpi_transport_env.sh`; scorecard | RULE | |
| 14 | 2026-07-28 | mu-convergence is non-monotone at 512b (-240/-183 meV c4951->c6947) vs +47/+38 meV at 256b | ladder record | OPEN — physics observation | |
| 15 | 2026-07-28 | `test_slate_cholesky_trsm_cpu` hangs; pre-existing, identical on the AW lib | scorecard | OPEN | |
| 16 | 2026-07-28 | zeta-apply performs a full-mu gather [1,4992,5760] under the distributed tier, O(mu*N_r/sqrt(P)) | scorecard | OPEN | |
| 17 | 2026-07-31 | fastloop mini-deck certified: full driver chain (kmeans->dipole->kin-ion->gw->eqp-convert->ht dft+qp) green at P=1 AND on a 4-host-device 2x2 mesh; p1 rerun exact-0 on every compared quantity, shard4 worst delta 1.15e-13 eV (sigma_mnk.h5); warm chain wall 76.6 s (p1) / 107.5 s (shard4); deck = MoS2 2x2x1 / 40b / 394c via the job-7884642 QE recipe | jobs 7884926 (deck), 7884936 (pin+check); `fastloop/pins_mini.json`, `fastloop/reference/`, src @ 24e4dc3 | GREEN — gate live, run before committing driver changes | |
| 18 | 2026-07-31 | `slab_io=auto` resolves to PHDF5_FFI whenever the host .so exports the handler, then aborts in MPI_Init_thread (MPIR_pmi_init) on a bare single-process launch with no PMI environment — auto does not probe MPI bootstrapability; mini-deck pins `slab_io=h5py_allgather` as the workaround; repo-side demotion fix reported, not patched | job 7884926 gw stage, `fastloop/work/build_minideck.7884926.out`; fastloop catch #1 | OPEN — repo-side fix needed | 21 |
| 19 | 2026-07-31 | `gw.gw_jax` completes main() (all outputs on disk, timing table printed) then hangs indefinitely in interpreter teardown on a bare P=1 launch (>=4 min silence observed); only chain driver affected (also the only one on the FFT/GEMM FFI paths); fastloop works around it with a post-main os._exit wrapper (`GW_WRAPPER`), the same pattern as the repo's failfast hook | job 7884928 (hang), 7884936 (wrapper green); fastloop catch #2 | OPEN — repo-side root-cause needed | 22 |
| 20 | 2026-08-01 | All seven chain drivers (gw_jax + kmeans_cli, get_dipole_mtxels, kin_ion_io, htransform, bse_jax, exciton_bands) routed through runtime.initialize_communicator_stack: one module-top startup call, zero direct prepare_mesh in drivers, canonical-mesh cache in collectives.resolve_mesh; numerically inert (fastloop check exit 0, parity exact-0 both legs except the known 1.15e-13 shard4 sigma_mnk) | job 7884986; repo e97e8ed; AST gates 34/34 + 68/68 + 9/9 | FIXED — subsumes 24e4dc3's interim routing | |
| 21 | 2026-08-01 | slab_io=auto probes MPI bootstrapability before either MPI tier (launcher PMI/PMIx env, else a throwaway-subprocess singleton MPI_Init probe that survives Intel MPI's abort) and demotes with a full announcement on bare launches; bare P=1 gw with slab_io=auto completes rc=0 on the demoted allgather tier, parity PASS; the mini-deck runs slab_io=auto as a standing regression test | jobs 7884987 (step 4), 7884989 (phases 0b/2); repo aef6710 | FIXED — closes 18 | |
| 22 | 2026-08-01 | gw_jax teardown hang root-caused: jax's atexit clean_up() destroys the XLA:CPU client and, after a fully-cold in-process compile storm (~195 XLA compiles), that destructor deadlocks in pool shutdown (main thread futex-parked, ~53 tf_XLAEigen workers spinning; Python atexit completes first). Deterministic in the fully-cold cell at BOTH 24e4dc3 and HEAD (2x 1200-s stage timeouts, thread census on disk); warm and small-cold runs clean in every FFI dial combination (6 runs). Fixed by runtime.finalize_process (ordered explicit teardown: effects barrier, unregister jax clean_up, distributed shutdown, remaining atexit hooks, announced os._exit) adopted by gw_jax __main__; fastloop check exit 0 with GW_WRAPPER REMOVED | jobs 7884986/7884987 (clean matrix), 7884989 (0a/0b hang repro + phase 2 acceptance); repo d3465cc; census fastloop/work/accept.7884989.d/hang_threads.004020.txt | FIXED — closes 19 | |
| 23 | 2026-08-01 | Replicated zeta-fit charge factor folded to a q-parallel P>1 SCHEDULE (same plan, same bits — deliberately NOT a third resolution; factor family stays replicated \| distributed): q scattered over all devices, whole per-q tiles, shared kernel `_charge_factor_math`, auto above nq·mu_log³ ≥ 5e9 (`LORRAX_ZETA_QPARALLEL` 0/1 override). b300 deck 8x2 A/B: zeta_fit.cholesky 104.372 → 11.755 s (8.9x; baseline 7884656 was 105.100 s), GW wall 335.9 → 214.6 s; parity max-delta 0.000e+00 on eqp0/eqp1/eqp_g0w0/sigma_diag/sigma_mnk.h5 for fold-vs-control, fold-vs-7884656 AND control-vs-7884656; bit-identity gate `test_qparallel_execution_is_bit_identical_to_replicated` max_abs = 0.0 (5 meshes × 2 modes, q-pad + mu-pad exercised); fastloop both legs PASS, mini-deck stays below threshold by design | jobs 7885021 (gates + fastloop), 7885024 (A/B, bundle 854af1f); repo 854af1f; harnesses + runs /scratch2/08271/jackmc/zeta_qpar/ | FIXED — closes KNOWN_LORRAX_ISSUES gw row 1 | |
| 24 | 2026-08-01 | Distributed zeta tier (`distributed_zeta_solve=distributed`, charge factor via ScaLAPACK pzheevd + 2D-sharded C⁺, back-solve one 2D-sharded GEMM) verified end-to-end on the b300 physics deck at the certified 8x2x28 / 4x4-mesh geometry: rc=0, GW wall 222.1 s; zeta_fit.cholesky 24.25 s (vs 11.76 s local q-parallel / 104.37 s all-ranks control, job 7885024 — expected slower at this size; the claim is correctness + memory shape), per-r-chunk back-solve 12.50 s vs 16.0 s local. Parity vs local tier AND vs 7884656 baseline: max\|Δ\| 8.573e-6 eV eqp0/eqp1, 9.000e-6 eV eqp_g0w0/sigma_diag, 4.936e-5 eV sigma_mnk.h5 — strict 1e-8 gate FAILS (block-cyclic eigh = different gauge, ~κ·ε with κ≤1/rcond=1e8, as documented), stated gauge tolerance 1e-4 eV PASSES, deltas identical against both references; truncation active at real conditioning (n_keep≈1995/2992 per q). Per-rank MaxRSS (sacct, sampled) 6.0 GiB vs 4.6 GiB all-ranks control — no O(nq·μ²) gather term; collective payloads: C⁺ site chunked 107.4 MB×4, GEMM site 485.2 MB/q with the documented ABANDONED-bound warning (loud floor, not a defect). Transverse channels: tier resolves to per_q by design; no standalone transverse factor stage exists — gap registered in KNOWN_LORRAX_ISSUES with design in repo docs/dev/large_nmu_operation.md | job 7885077; harness /scratch2/08271/jackmc/zeta_qpar/job3_b300_dist.sbatch; run run_b300_gw_dist; bundle 833af17 | VERIFIED | |
| 25 | 2026-08-01 | ζ-fit provenance records the `distributed_zeta_solve` GAUGE tier ('replicated' \| 'distributed'; per_q collapses to replicated — back-solve gather granularities over the same factor bits) with NO forced refit of existing ζ: schema stays 1, reader treats a MISSING key as legacy replicated (one-line notice, reuse allowed) and refits on real tier mismatch. Reader matrix on the REAL b300 legacy ζ: byte-identical→reuse; legacy+replicated→reuse+notice; legacy+distributed→refit with tier message; legacy+tier+changed-cutoff→refit. End-to-end (leg C, copied run dir): 2-key diff (tier + wfn_file) took the generic refit path with both keys named; refit outputs exact-0 vs control on eqp0/eqp1/eqp_g0w0/sigma_diag | jobs 7885105 (unit matrix + fastloop both legs PASS), 7885109 leg C; repo 8b02c72 | FIXED — closes KNOWN gw provenance row | |
| 26 | 2026-08-01 | Σ τ-loop async-D2H deque persists across window boundaries — one flush per branch, FIFO, bit-identical (unit accumulator matrix incl. a window shorter than lag: totals BIT-IDENTICAL to flush-per-window, sink order exact). b300 8x2 leg A vs job-7884656 baseline: EXACT-0 (0.000e+00) on eqp0/eqp1/eqp_g0w0/sigma_diag AND sigma_mnk.h5; sigma.exec 71.222→70.605 s; boundary flush 16 drains/5.77 s → 8 drains/2.64 s under sigma.finalize. CONFIRMS the stage is DEVICE-bound: BC's "88% d2h_wait" was the τ-kernel device wall surfacing in the drain (the ppm_accumulators comment warned this 2026-07-28), NOT missing overlap | jobs 7885105 (units + fastloop p1/shard4 exact-0 / known 1.15e-13), 7885109 leg A; repo 8a9a21c | FIXED — corrects the BC reading | |
| 27 | 2026-08-01 | Probe-χ₀ reuse `ppm_probe_chi_reuse=auto` (deck key, DEFAULT off): probe χ₀ accumulated inside ONE fused τ sweep on static + k augmented nodes drawn greedily from the dedicated quadrature (Lawson-weighted fits, gate = max(dedicated err, target), guaranteed exact-dedicated fallback). Weights-only refit on static nodes alone is provably insufficient: probe integrand = Laplace transform of cos(ωp t), static 1/x grid cannot resolve the τ-tail oscillation (2.6e-4 vs dedicated 1.3e-6). b300 8x2 leg B: fused 10+4 nodes (dedicated pass = 8), probe err 2.4e-6 ≤ gate 4.6e-6; chi0_W_probe 9.65 (BC) / 6.78 (control) → 3.56 s (chi.reused 0.0) — BUT greedy planning costs 1.79 s host, so screening NETS +1.2 s at b300 scale (16.16→17.34 s); pays only where per-node χ cost dominates the fixed planning cost (larger μ). Deltas vs control: eqp0/eqp1/eqp_g0w0/sigma_diag ≤ 8.7e-5 eV, sigma_mnk.h5 ≤ 1.039e-4 eV (marginally over a 1e-4 gate on off-diag Σ_total). Mini-deck P=1: probe row 0.592→0.122 s, eqp ≤ 1.49e-5 eV. Stays opt-in; default flip = owner call AFTER cheapening the planner + re-pinning baselines | jobs 7885097 (LS insufficiency), 7885105 (gate matrix ωp 0.5–8 Ry, k=2–5; mini end-to-end), 7885109 leg B; repo 64b17c3 | MEASURED — opt-in; no net win at b300 scale | |
| 28 | 2026-08-01 | htransform Galerkin factorization rewritten as Gram-eigh of A Aᴴ (nk·nb square, N_mu-free) through the ffi.linalg eigh plan (deck key `eigh_backend`; auto=native replicated, distributed=pzheevd), Vᴴ and B_at_mu mu-sharded on 'y' (B fitted at true n_mu); rank-0 writer gate on bandstructure.dat; Γ-label compared canonically (old literal-'\\u0393' comparison never matched; argmin fallback survived on the norm-0 pad-row tie). Gauge-class change, measured EXACT at every gate: fastloop p1+shard4 PASS with bandstructure dft/qp max\|d\|=0.0 both legs; b300 P=1 dft+qp AND srun P=4 (mlx, 1 node × 4 ranks) bandstructure.dat max\|d\|=0.000e+00 all 7 columns vs pinned baselines (gauge deltas below the 1e-8 print); rank 704/704 retained, identical to SVD. Memory (1 proc × 4 host devices, dft): mu 4962→18084 process-VmHWM slope +1.9 GB (ctl 18b1917) → +1.2 GB (new = exactly the sharded psi/Vh/B sum-of-shards terms; replicated A+gesdd term gone), wall 67-70 → 44-45 s; P=4 srun per-rank VmHWM 3.04-3.34 GB. Remaining mu-bound: single-axis psi-at-centroids sharding (centroids row, open). sqrt-of-eigenvalue precision note recorded at the cut (retained spectra measured 4.5 decades above it) | jobs 7885090 (fastloop), 7885093 (b300 matrix, harness /scratch2/08271/jackmc/ht_gram/job1_ht_gram.sbatch); repo 62ba395 | FIXED — closes KNOWN_LORRAX_ISSUES htransform rows 1-3 | |
| 29 | 2026-08-01 | Transverse ζ factor HOISTED (branch `zeta-transverse-hoist` @ f695bf4 on 5da517b; NOT merged — facade/C++ workstreams hold uncommitted edits on the same ffi files): `factor_c_q` runs the per-q ridged pivoted LU ONCE per channel and returns (factor, piv); `solve_zeta` only applies it per r-chunk. LOCAL plan = `lax.linalg.lu` on the whole logical tile (q-parallel under the charge fold policy) + `jax.scipy lu_solve` — a SCHEDULE, bit-identical to the fused per-r-chunk `jnp.linalg.solve`: unit gate `test_transverse_factor_hoist` EXACT (3 meshes × 2 gather tiers × 2 schedules × 2 r-chunks, q-pad + μ-pad, TRS near-null fixture) AND production bispinor A/B EXACT-0 on eqp0/eqp1/eqp_g0w0/sigma_diag/sigma_mnk.h5 at bi4 (402c+143T, P=4/2×2) and bi16 (785c+275T, P=16/4×4). Controls also reproduce the archived AX baselines: exact-0 .dat, 1.137e-13 eV h5 (merged branch + certified FFT-FFI stack vs wt-BC env). Measured: bi16 per-channel zeta_fit.chunk.solve 4.3–12.8 s → 1.7–2.8 s (re-factorization gone), GW wall 214.3 → 201.2 s; scales as n_rchunks·P at production μ_T. cusolvermp (CUDA) keeps the fused pair (port pending; fused branches preserved under lu_piv=None) | jobs 7885122 (unit gates 10 passed incl. 2 new), 7885126 legs A/B/C/D; fastloop both legs PASS 7885123 (worktree src + new .so) | FIXED — closes KNOWN gw transverse row | merged 6f162f6 → 32 |
| 30 | 2026-08-01 | ScaLAPACK LU pair SPLIT: new host handlers `lorrax_scalapack_batched_getrf` (block-cyclic factors + per-rank int32 ipiv out) / `_getrs`; hoisted distributed transverse factor runs pXgetrf once per channel, pXgetrs per r-chunk with ipiv threaded verbatim (never gathered). Split-vs-fused BIT-IDENTICAL: contract cell `scalapack_getrf_getrs` exact (incl. factor reuse across two RHS) on the 2×2 process mesh (impl=mpi, both dtypes; 1×1 pytest form dies silently at import with the .so present — pre-existing bare-launch MPI-init class, CLI matrix is the instrument) and production E/F legs (402c+136T, P=4) EXACT-0 on all five outputs. Distributed-vs-local gauge (F vs G): 0.0 at .dat print precision, 1.137e-13 eV sigma_mnk.h5 — far under the 1e-3 eV significance bound at this size. Resolve now probes all three handlers (old .so refuses, rebuild build_ffi_host.sh); .so build_host_TLU sha 5e0ba705…, symbol table = ONE + the 2 new handlers. CLI entry fixed for cpu+impl=mpi (eager slate MPI warm-up now CUDA-only — double MPI_Init abort, 7885123); post-verdict "MPI routine after finalizing" teardown rc=1 in CLI mode remains (cosmetic, verdict line is the record) | jobs 7885123/7885125 (build + CLI matrix "done: 0 failures"), 7885126 legs E/F/G; repo branch commits 6571c04, 2fca4b4 | FIXED | merged 6f162f6 → 32 |
| 31 | 2026-08-01 | Mesh-divisible transverse centroid set for the 4×4 deck: `centroids_T_div136.txt` (136 pts = %8==0 — divides 2×2, 4×4 and 8×8 mesh axes; kmeans_cli 130 --orbit --density-mode current --qe-save out/MoS2.save --seed 0, same provenance header as centroids_T_t134), at /scratch2/08271/jackmc/zeta_T_hoist/. Ladder observation: requested 128-138 delivered {133,136,138,141,145} — orbit closure makes exact divisibility a search, ~1/6 hit rate at this size. The ~272-pt divisible set (bi16-scale scalapack) NOT yet generated — deferred. Deck-dir placement deferred (mos2_4x4_test treated read-only, in use by another agent) | job 7885126 step -1; job 7885122's ladder invalidated (leaked JAX_COORDINATOR_ADDRESS → kmeans joined the CLI leg's coordinator, DEADLINE_EXCEEDED; job cancelled) | GREEN | branch merged 6f162f6 → 32 |
| 32 | 2026-08-01 | Branch `zeta-transverse-hoist` (f695bf4) MERGED into fix/zq as 6f162f6 (+ 96d7996: build_ffi_host.sh symbol gate learns the split pair) and CERTIFIED. Conflicts (branch base 5da517b predates the wave-2 facade commits 7d939be/d992634) resolved as content supersets: getrf/getrs implementation ported VERBATIM into `ffi/linalg/_scalapack.py`, `ffi/scalapack/{__init__,solve_lu}.py` stay re-export shims carrying all three LU names; branch-new `getrf_getrs_ffi.cc` given the seam-audit explicit includes (`<string>`, `../slate/ctx.h`); CMakeLists + large_nmu docs auto-merged clean. Host .so rebuilt ON LOGIN from the merged tree (build_ffi_host.sh, gcc/12.2.0 module + cmake 4.1.1 + venv ninja capped -j4, LORRAX_XLA_FFI_INCLUDE_DIR pointed at the venv jaxlib headers — no container, no venv python): link clean under `-Wl,--no-undefined`, ZERO compile warnings (merged SYSTEM-include/no-Wno-unused regime holds on the new TU), 17/17 handler-symbol gate incl. all three LU handlers; `build_host_MRG` sha b691abc6…, dynamic symbol table IDENTICAL to certified build_host_TLU (25 names). Merged-tree gates: login AST 68/68+34/34+9/9+46/46+4/4, py_compile clean on every touched file; unit gates 10 passed (transverse hoist bit-identity EXACT, no .so exported); CLI contract matrix 2x2 P=4 with the MRG .so "done: 0 failures" — 6/6 PASS incl. `scalapack_getrf_getrs` both dtypes; fastloop check BOTH legs PASS. bi4 spot A/B SKIPPED — nothing numeric attributable to the merge: fastloop deltas (kin_ion.h5 1.421e-14 v_hartree, sigma_mnk.h5 2.274e-13, both legs) are DIGIT-IDENTICAL to the pre-merge HEAD-side certification job 7885116 (kernel-cleanup workstream) — HEAD provenance, not merge drift — and every merge-side instrument is bit-exact. wt_zetaT worktree removed (clean); branch ref kept | job 7885137 (/scratch2/08271/jackmc/zeta_T_merge/: job out, cli log, ffibuild_login.6f162f6.log, .sym_mrg/.sym_tlu); repo 6f162f6 + 96d7996 | GREEN — merge certified | |
| 33 | 2026-08-01 | SQUARE-MESH ruling implemented as REFUSAL, not idle-rank truncation (decisions.md 2026-08-01): `resolve_mesh` builds s×s and refuses a non-perfect-square device count naming s² and (s+1)²; `create_mesh_2d`/`create_mesh_xy`/`RuntimeStack.reshape` refuse px≠py; rectangular accommodation deleted (most-square factorisation loops ×2, sharding_fit larger-product tie-break). Idle ranks are NOT implementable without deep surgery on this stack — three independent deadlock mechanisms: impl=mpi communicator creation is MPI_Comm_split, collective over MPI_COMM_WORLD (out-of-mesh ranks never enter the warm-up jits → in-mesh ranks block in the split); psum_replicate + k-sweep gathers hard-assume mesh size == world; multihost_utils barriers span the WORLD. Probes: 4 host devices → 2×2, 3 devices → refusal naming 1 and 4; b300 8×2 (P=16) → 4×4 mesh, warmed | job 7885145 phase 1 (probes) + 7885146 (4×4 banner both legs); repo commits 6da7f69, 73a9013; /scratch2/08271/jackmc/ffi_required_sweep/ | RULE — deviation from the ruling's truncation letter, per its no-deadlock spirit; owner may re-open | |
| 34 | 2026-08-01 | FFI-REQUIRED ruling landed (decisions.md 2026-08-01): all three gates default ON; startup refusal via Gate.enforce (runtime step 6b) names the .so + docs/environment page; `auto` tier DELETED from gate.py; per-knob `=0`: LORRAX_FFT_FFI REFUSES (XLA flat-k twin deleted from make_flat_k_fft), LORRAX_FFT_FFI_FUSED=0 announced opt-out to the decomposed (still-FFI) chain, LORRAX_BANDS_GEMM_FFI=0 announced UNCERTIFIED opt-out (XLA einsum arm retained for structural extra='minor'; minor keeps XLA quietly under every mode incl. =1, which used to refuse); grammar errors resolve to the DEFAULT announced (off may refuse, a typo must not kill a run). KEPT per ruling: vendor fallbacks inside handlers, BSE XLA FFTs + local_*fftn3 (no FFI route), slab_io h5py tier. Verified: probes 7/7 (defaults, enforce green, =0 semantics, missing-.so refusal, grammar); unit gates test_contract_bands 9/9 + test_runtime_startup_report 51 + test_sharding_fit 16 + test_collectives_distribution 38/1skip (7885148, after fixing its PRE-EXISTING stale prepare_mesh pin — failed at base 96d7996 too); fastloop check BOTH legs PASS with NO gate exports (runner exports removed — every run now exercises required semantics end-to-end); login AST 68+34+9+46+4. b300 GW A/B 8×2: leg A = base 96d7996 bundle + explicit certified stack, leg B = sweep d4ce5ed bundle + new defaults — EXACT-0 on all five outputs (max delta 0.000e+00 incl. sigma_mnk.h5), and B exact-0 vs the latest on-disk baseline (gw_perf ctrl, 7885109) and A exact-0 vs pinned 7884656; walls 216.9 vs 223.8 s (noise). LOC: src/ 333 deleted / 469 added (435/707 with tests+docs) | jobs 7885145 (probes+units+fastloop), 7885146 (A/B, /scratch2/08271/jackmc/ffi_required_sweep/run_b300_gw_{base,sweep}), 7885148 (cdist re-test); repo commits 4b67ebd (fft), ee44a00 (gemm), d4ce5ed (docs), 73a9013 (test fix); bundles lorrax_bundle_{96d7996,d4ce5ed} | GREEN — rulings landed | |
| 35 | 2026-08-01 | P1.2 GPU leg CERTIFIED on rtx-dev under the FFI-required defaults (NO gate exports anywhere). (1) BUILD: first-ever object compile of the COMMITTED unified-tree cuFFT TU (`src/ffi/cpp/cufft/fft_flat_k_cuda_ffi.cc` — present since d992634/34c5c46, previously built only from the Jul-29 pre-unification working tree) via `config/frontera/build_ffi.sh` CUDA leg from a frozen 73a9013 snapshot against the production cuda12 wheel toolkit (12.9 line, cuFFT 11.4.1.4, driver 535/sm_75): clean in 41 s, "cufft: strided flat-k FFT handlers ON", symbol gate 5/5 (CufftFlatKCudaFfi, CufftGwConvCudaFfi, PhdfWriteFfi, PhdfReadKchunkUnionFfi, EighMpFfi), DT_NEEDED gained exactly libcufft.so.11+libnvrtc.so.12 -> `$WORK/lorrax_ffi_p12_cuda/build_phdf5/liblorrax_ffi.so` (deps via symlink to the certified cufft stage; venv untouched). (2) SMOKE, PURE DEFAULTS: bare-input g108 (4x4 deck, c108) 1 node x 4 RTX 5000, rc=0 in 120 s; eqp0 AND eqp1 vs pinned CPU baseline run_am_p1 max\|Δ\|=0.00e+00 over 5160 values (tol 2e-5 eV); startup report shows the intended CUDA resolutions — `[fft_ffi] ... cuFFT strided CUDA FFI handler` fired for BOTH targets (flat_k AND gw_conv), slab_io=auto -> PHDF5_FFI (single-node CUDA writer), 0 gloo lines, NCCL collectives. (3) ABSENT LIB (LORRAX_FFI_SO unset): startup REFUSAL exactly per decisions.md — rc=1 in 6 s, Gate.require RuntimeError naming liblorrax_ffi.so, LORRAX_FFI_SO, the verbatim probe reason ("the CUDA FFI library could not be loaded: FileNotFoundError ...") and decisions.md 2026-08-01; fail-fast carried it to the job exit code; NO outputs, NO silent fallback. (4) DEFECT found+FIXED: startup-report dial sentences were platform-blind — on CUDA the block claimed LORRAX_BANDS_GEMM_FFI was "routed through the FFI handler (the required layer)" though the dial does not exist there (host symbol table only; enforce skips silently by declared policy; GEMMs ride XLA:GPU cuBLAS). format_startup_report now states the platform truth (test pinned); verify job: pytest test_runtime_startup_report 52 passed, g108 defaults re-run rc=0 (129 s) with the corrected sentence live + eqp0 exact-0 again | jobs 7885151 (build+smoke+absent), 7885153 (fix verify); repo commit bbe6e56; artifacts /scratch2/08271/jackmc/lorrax_sandbox/runs/p12_gpu_cert/ (srcsnap_73a9013, srcsnap_p12fix, logs/, run_g108_defaults{,_fix}, run_g108_absent, p12.7885151.out, p12fix.7885153.out) | GREEN — GPU leg certified | |
| 36 | 2026-08-01 | P1.1 FULL pytest census on Frontera CLX in-container, FFI-required defaults, host .so build_host_MRG: bare leg 735 passed / 24 failed / 56 skipped; ffi_linalg_contract under srun --mpi=pmi2 + mpi_transport_env.sh GREEN (27 passed / 25 skipped — the CLAIMS-30 "1x1 pytest dies at import" class is the MISSING PMI2 GLUE: bare and transport-env-less srun die, transport-env srun is green); 4-device leg 144/1/2 with C2 nonsquare-refusal green; -m extra tier 21/0/5. Every non-passing test triaged in repo tests/KNOWN_FAILURES.md; fixes landed for: zeta_is_done stale test builders (13), _MockPsiGStore._bpd_per_bc (6), G2 window pin (stale vs d011a36, regenerated), gnppm/bispinor Tier-1 pins re-frozen on this platform (max delta EXACTLY 1e-6 eV = one unit in the 6th printed decimal, sigX exact-0; Tier-2 invariances + both COHSEX gates green), runtime skip_gpu_plugin_discovery demotion gap (too-late + done branches now pin JAX_PLATFORMS=cpu), charge_zeta_route refusal re-pinned above the R15.1 per-q-batch cap, contract_bands/projection_lgemm assert→skip. ONLY remaining red: ring-transport scan-vma class, 10 tests (bse_ring_comm.py:382 fori carry unannotated — the old handoff's "bse_ring_comm vma"; KNOWN_LORRAX_ISSUES bse row added). INSTRUMENT: apptainer drops host LD_LIBRARY_PATH — export INSIDE the container or the FFI gate refuses everything (job 7885150's 97 reds, superseded) | jobs 7885150 (instrument lesson), 7885154 (authoritative; junit XMLs + artifacts /scratch2/08271/jackmc/pytest_p11/); repo tests/KNOWN_FAILURES.md | GREEN census — release ships the listed known-fails only | |
| 37 | 2026-08-01 | b600/P=64 scale shakeout on the MoS2 4x4 deck: the ENTIRE driver chain (kmeans -> dipole -> kin-ion -> gw -> htransform dft+qp) runs GREEN at 600 chi/W bands, 4775 orbit-closed charge centroids and 64 ranks (32 nodes x 2 x 28, 8x8 mesh, impl=mpi, provider mlx, compile cache ON, slab_io=auto -> PHDF5_FFI collective writer). rc=0 everywhere, NO OOM, NO refusal, NO traceback; whole chain ~7 min of 32-node allocation. Walls / per-rank peak VmHWM: kmeans 51 s / 10.61 GiB (4775c from a 4800 request, rank gate 431/431 PASS, selection window (0,26)x(0,600)); dipole 22 s / 2.10 GiB; kin-ion 17 s / 2.15 GiB; GW 250 s / 16.24 GiB; htransform dft 28 s / 2.31 GiB and qp 18 s / 2.22 GiB with the EQP override PROVEN taken from the log. mu_pad = round_up(4775,64) = 4800 AUTOMATICALLY via Meta.n_rmu_padded — no %8 divisibility ladder was needed or used (the div136 precedent applies only to the distributed transverse LU, not the charge channel). No NSCF was needed: WFN_b1024.h5 is the same 30 Ry / 4x4x1 lineage and nband=600 is honest because load_centroids_band_chunked zeroes psi(G) on bands [600,640). E_DFT parity vs the b300 run EXACT-0; Eqp1 shifts mean 0.197 / max 0.321 eV for the 300->600 band-sum step. kmeans and htransform both ran at P=64 with NO LORRAX_MPI_FORCE_THREAD_MAIN — the e97e8ed warm-up fix holds at 64 ranks and BD.3's retry lever is retired | jobs 7885312 (bundle), 7885313 (P=4 harness smoke), 7885315 (deck), 7885316 (GW), 7885322 (htransform); artifacts + full stage tables /scratch2/08271/jackmc/b600_p64/ (RESULTS.md, logs/, harness/, lorrax_frozen @ 1fc2759) | GREEN | |
| 38 | 2026-08-01 | b600/P=64 vs b300/P=16 GW stage scaling (jobs 7885316 vs 7885024; 1.6x mu, 2.0x bands, 4x ranks; overall wall 214.5 -> 240.6 s = 1.12x). SCALES AS OR BETTER THAN EXPECTED: sigma.exec 70.69 -> 70.85 s (1.00x, flat — device-bound, per-rank work constant); zeta chunk_loop 93.00 -> 33.48 s (0.36x) with z_q_build 76.44 -> 22.84 s (0.30x vs an nb*mu/P expectation of 0.80x); chunk.solve 16.02 -> 10.20 s (0.64x vs mu^2/P = 0.65x); chi.exec 4.99 -> 4.64 s (0.93x vs 1.29x); real W.exec 4.05 -> 5.21 s (1.29x vs a no-P-gain mu^3 expectation of 4.13x). WORSE THAN EXPECTED, ranked: (1) load_centroids.loader_load 0.19 -> 5.05 s = 26.5x against ~8x more bytes — per-rank whole-file h5py WFN reads, superlinear Lustre contention, the only term degrading faster than P grows; (2) zeta_fit.cholesky 11.76 -> 53.71 s = 4.57x vs mu^3 = 4.13x, now 22.4% of the GW wall with 54/64 ranks idle (P=nq ceiling, see row 39); (3) chi0_W_probe 8.08 -> 16.35 s = 2.02x, now 1.44x the real screening pass. Per-rank peak memory 4.63 GiB (sacct, b300/P=16) -> 16.24 GiB (VmHWM, b600/P=64) = 3.5x growth at 4x the ranks, perfectly flat across all 64 ranks (16.13-16.24 GiB, no gather hotspot); no nq*mu^2 BAND-gather class present. ISDF planner: 33.99 GB/dev estimated vs 4.63 GiB measured at b300/P=16 (7.3x OVER, and it cost an extra r-chunk) vs 16.43 estimated / 17.44 GB measured at b600/P=64 (1.06x) | jobs 7885316, 7885024, 7885077; scorecard BD.2 for the non-GW b300 P=16 baselines; /scratch2/08271/jackmc/b600_p64/RESULTS.md §2-§4 | GREEN — measured scaling map | |
| 39 | 2026-08-01 | The charge zeta-factor q-parallel fold saturates at P = nq_ibz, and the distributed tier's documented wall/memory trade is BACKWARDS at production shape. (a) CEILING: at nq_ibz=10 / P=64 the local plan announces `nq=10 scattered over 64 devices ... q-pad 54` — 54 of 64 ranks idle for the entire 53.7 s factor; the fold was already at 1 q/rank at P=16, so P=16->64 bought this stage nothing and its 4.57x growth is pure mu^3. `docs/dev/large_nmu_operation.md` presents the fold without stating this ceiling. (b) CROSSOVER: with ONE deck key changed (distributed_zeta_solve = distributed, job 7885323, same geometry/deck/centroids), zeta_fit.cholesky 53.71 -> 32.74 s (0.61x), chunk.solve 10.20 -> 6.68 s, screening 31.02 -> 24.05 s, GW wall minus startup 214.8 -> 177.6 s (0.83x). The doc places the wall crossover at mu=10015 on 64 ranks and calls the tier 2.1x SLOWER at b300/P=16 — at mu=4775/P=64 it is already 1.64x FASTER on the factor. The crossover is governed by P/nq, not mu alone. (c) NO MEMORY BENEFIT AT THIS SHAPE: per-rank peak VmHWM 16.24 -> 16.22 GiB, unchanged, although the local tier's announced 3.69 GB/rank replicated (nq,mu,mu) gather is entirely absent from the distributed one — the peak binder is the C_fit_one_rchunk transient, so relieving the gather moves nothing. Startup was netted out honestly (local leg 201 cold compiles / 12.58 s cache arm, distributed leg 31 / 2.88 s warm). Local-vs-distributed parity: max|delta| 1.43e-5 (eqp0), 1.10e-5 (eqp1), 1.50e-5 (eqp_g0w0), 1.40e-5 eV (sigma_diag) — the documented kappa*eps gauge class at the run's own kappa/q = 9.98e7, ~70x under the 1e-3 eV significance bound, and the same class as the b300 measurement (8.6e-6 - 9.0e-6 eV) | jobs 7885316 (local) / 7885323 (distributed), run dirs /scratch2/08271/jackmc/b600_p64/run_gw{,_dist}/, RESULTS.md §6 | GREEN — measurement; two KNOWN_LORRAX_ISSUES gw rows opened | |
| 40 | 2026-08-01 | `sacct` MaxRSS is NOT a memory instrument for a harness that runs python as a CHILD of the container shell: it accounts the `apptainer` wrapper, not the driver. Job 7885315 (three drivers, 64 ranks): sacct MaxRSS 15800K / 26928K / 18336K against real per-rank /proc VmHWM peaks of 10.61 / 2.10 / 2.15 GiB — a ~700x undersample that reads as a plausible small number rather than an error. Historical LORRAX sacct figures ARE trustworthy (b300 4.63 GiB, job 7885024; b300 distributed 6.00 GiB, job 7885077) precisely because `config/frontera/templates/gw_dev.sbatch` `exec`s python as the srun task. Any harness that must sample the child (as this one did, to read VmHWM) has to carry its own /proc sampler and must not quote sacct. Independent of, and additional to, the AY brief-peak caveat (L9828) | jobs 7885315 vs 7885024/7885077; harness /scratch2/08271/jackmc/b600_p64/harness/inner_common.sh (`lorrax_run`) | RULE | |
| 40 | 2026-08-01 | Bispinor q=Γ tile treatment MEASURED (4-leg restart-doctored check on bi4, 402c+143T, P=4, src @ 1fc2759+2678f61): CC head correct as implemented; CT/TC exactly zero at the bare level (Coulomb-gauge property of the propagator, independent of SOC vertices); TT tiles MISS a finite G=0 mini-BZ head. Leg A bit-identical (CMP_TOL=0) to certified 7885126 run_bi4_new AND exact-0 vs archived run_AX_bi4; AR-vs-A restart reproducibility exact-0 on eqp0/eqp1/eqp_g0w0/sigma_diag/sigma_mnk.h5. Γ-zeroed leg B: whole q=Γ TT term = eqp/Σ_X max\|Δ\| 0.347 meV, −0.122 of the −1.553 eV Σ^B trace (7.8%). Head-injected leg C: missing correction = max\|Δ\| 0.209 meV (mean 0.037 meV), Σ^B trace −0.076 eV (4.9%), off-diag tiles unaffected. Mini-BZ averages ⟨v·t^{ij}⟩ = (0.4993, 0.5007, 1.0000)·vc0 diag (vc0 = 2443.3 a.u. bare @4x4), ⟨v t^{12}⟩ = 4e-4·vc0, t^{i3} ≡ 0; ζ_T(Γ,G=0) norms 1.17–1.21e3; head/Γ-slab Frobenius 0.97/1.04/6.0 (11/22/33). Correction decays ~1/√N_k (2D cusp class, same as the corrected CC head) | job 7885325; /scratch2/08271/jackmc/bispinor_gamma_check/ (doctor_report.7885325.json, gamma_check_analysis.7885325.txt, run_{A,AR,B,C}, doctored vq_{B,C}*.h5); BISPINOR_DHFB_DESIGN.md §11, merged to fix/zq @ 4cc65ac | GREEN — measurement; KNOWN_LORRAX_ISSUES bispinor row opened, SUP for adopting the fix | |
| 41 | 2026-08-01 | Bispinor assembly memory audit at the 4800C+1600T production target (nq=nk=16, ns=4, b600-class, P=64): NO (μC+μT)² four-tile concatenation exists anywhere in src/ — the ~650 MB/q full Lorentz block is never built (phase-2 Dyson must keep per-tile contraction). Production FFI tile reads are sharded /P (10.2 MB/rank TT, 92 MB/rank CC @P=64). Ranked risks, all registered: (1) h5py_allgather fallback replicates reads — CC tile 5.9 GB ×2 per rank; (2) Σ-stage absent from plan_gflat_chunks — charge G (nk,4,μ,4,μ) ~4.4 GB/rank today absorbed by headroom, Σ^B adds 0.5–0.66 GB/rank ×9 sequential pairs; (3) TT Lorentz-mix ~27 concurrent tile-equivalents (276 MB/rank @P=64, 4.4 GB/rank @P=4); (4) ψ bundles √P single-axis class (T-bundle 492 MB/rank persistent); (5) padded_mu_extent multiple-of-P pad quadratic at P ≳ μ (1600→4096 @P=4096 = 6.6×); (6) latent _zero_tile single-device eager zeros (~2 GB/process) on the dormant CT path. Sharding probe (2x2 mesh): eager jnp.pad AND conj∘swapaxes both PRESERVE the mu-sharded spec (pad/Hermitian read paths are NOT replication sites; Hermitian yields P(None,'y','x') → one all-to-all at the consumer); bare jnp.zeros = SingleDeviceSharding confirming the _zero_tile transient | audit + probe job 7885325 (probe_sharding.7885325.txt, job_gamma_check.7885325.out); 7 KNOWN_LORRAX_ISSUES rows (bispinor 4, gw 1, cross-cutting 2) | GREEN — audit | |
| 42 | 2026-08-02 | Charge-vs-bispinor performance parity audit (READ-ONLY, src @ 4cc65ac + unmerged worktree `zeta-transverse-ranktrunc` @ 340686f): the ζ-FIT path is at parity — charge and transverse share `c_q_from_psi_sm`, `z_q_from_psi_sm`, `fit_one_rchunk`, `_resolve_zeta_gather`, the SlabIO G-flat writer and the compile cache; the factor hoist (6f162f6) and the q-parallel fold (`_qparallel_factor_ok`, same gate function, announce at core.py:2236) were ported verbatim, and 340686f ports the rank_truncate family by REUSING `_charge_factor_math`/`_factor_c_q_replicated_qparallel`/`_factor_c_q_distributed_rank_truncate(indefinite=True)`. Σ^B is NOT at parity: it carries none of the four charge Σ_c mechanisms (fused `gw_conv` FFI — only construction site is ppm_tau_kernel.py:256; `contract_bands_block_reshard` — 4 call sites, none bispinor; the lag-2 persistent async D2H deque; `precompile_sigma`), holds 3 G slots where the τ kernel holds 1, runs 9 tiles for 6 unique with ψ rebuilt 9x instead of 3x, blocks the host 18x per call, and has ZERO `timing.section` (0 in sigma_x_bispinor.py / cohsex_sigma.py / v_q_bispinor.py vs 7+10 charge-side). χ⁰/W have NO transverse counterpart anywhere in src/ (zero `bispinor \| transverse` hits in w_isdf/screening/minimax_screening/ppm_*) — N/A by structure, Σ^B is bare+static per BISPINOR_DHFB_DESIGN §8. Projections at 4800C+1604T/600b/P=64/ns=4: Σ stage unplanned by `plan_gflat_chunks` (peaks end at F_tensor_write) ≈4.6 GiB/rank (74 GiB/rank at P=4 vs a 96 GB budget); fused-FFI miss +338 MiB/rank (+5.3 GiB/rank at P=4); Lorentz mix 285 MiB/rank vs a 54.9 MiB Stage-E model; ζ reuse disabled for the whole bispinor run (gw_init.py:503) costs a ~500-800 s refit per same-dir rerun; ψ(G) whole-window read 2x + psi_G_store 4x = 12.3 s vs 5.6 s measured-charge. LIVE CONSTRAINT CONFIRMED: the b600 bispinor transverse set is 1604 points and 1604 % 8 = 4, so `distributed_lu=scalapack` is unreachable on the certified 8x8 mesh — 340686f's padded-extent pzheevd route is the fix. Wall projections are extrapolations (method + ±40% stated in PARITY.md) and are settled by job 7885953, which runs this exact shape | read-only audit of /work2/08271/jackmc/frontera/lorrax @ 4cc65ac and /work2/08271/jackmc/frontera/lorrax_wt_ranktrunc @ 340686f; anchors jobs 7885316/7885024 (charge b600/b300), 7885325 (bi4 timing table), CLAIMS 29-32/38-41; artifact /scratch2/08271/jackmc/bispinor_parity_audit/PARITY.md; 12 KNOWN_LORRAX_ISSUES rows added (gw 7, bispinor 5) | GREEN — audit; no code changed, no job submitted | |
| 43 | 2026-08-02 | kmeans `setup.weight` distributed (repo 920665f on fix/zq): band-chunk split + ONE psum replaces the per-rank replicated band×k sweep. Measured at P=4 on the b600 deck (grid 24×24×80, 10 stored k, 600 bands): sweep 11.4–13.2 s → 4.0 s (2.85–3.30x); weight delta vs the replicated sweep at identical chunking 4.4e-16 abs / 1.6e-16 rel (1 ulp), vs legacy chunking 2.2e-15 / 8.2e-16. TWO design corrections came from measurement, both recorded in KNOWN: chunk budget alone gave 3 chunks (split capped at 3 ranks, 0.70x = slower, job 7885968) and per-chunk psum cost 0.85 s/call (12x slower on the sparse leg, ~50 s of latency projected at P=64, job 7885969). END-TO-END GATE: `kmeans_cli 400 --orbit --seed 0 --weight-bands 0:600` at P=4, new tree vs frozen 1fc2759, centroids_frac_396.txt **byte-for-byte identical** | jobs 7885968/7885969/7885973 (design iterations), 7885975 (authoritative: A/B PASS both legs + centroid gate IDENTICAL); harness /scratch2/08271/jackmc/kmeans_weight_dist/{wab.sbatch,weight_ab.py} | VERIFIED | repo 920665f |
