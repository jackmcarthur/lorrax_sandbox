# CLAIMS.md — measurement and decision ledger

One line per claim. Append; never delete. When a claim is overturned, set
its verdict to REFUTED or add the superseding row's number to its last
column — do not edit the statement. A claim not in this ledger, or without
a jobid/artifact, is a hypothesis: test it, do not cite it. Deep context
for any row: grep `/scratch2/08271/jackmc/lorrax_setup/docs/SPEEDUP_SCORECARD.md`
for the jobid.

Verdicts: GREEN (measured, holds) | REFUTED | RULE (binding practice,
evidence-backed) | FIXED (defect closed) | OPEN.

| # | Date | Claim | Evidence | Verdict | Superseded by |
|---|---|---|---|---|---|
| 1 | 2026-07-31 | Full 4x4 GW pipeline on the vendored template: rc=0 in 2:37; eqp parity exact-0; sigma_mnk.h5 parity 1.1e-13 eV vs pinned baseline | jobs 7884609, 7884612 | GREEN | |
| 2 | 2026-07-31 | W-densifier compiles with 0 gather-class collectives in both output modes; numeric parity 0.000e+00 | job 7884609 probe step; repo `tools/probe_w_densifier_hlo.py` | GREEN | |
| 3 | 2026-07-28 | jax CPU collectives `mpi` green at AQ 4962c/P=64, both distributed tiers forced; Dyson residual <= 7.5e-15 | jobs 7877754 (warm, 554 s), 7877789 (cold, 514 s) | GREEN — production transport | |
| 4 | 2026-07-28 | gloo/ib0 fails reproducibly at P=64 distributed tiers (ReduceScatter tcp read timeout, gloo `buffer.cc:72`); also observed silently corrupting ~5% of reduce-scatters | jobs 7877753 (warm, rc=1 @211 s) + 7877761 (cold, rc=1 @227 s); scorecard §AY | REFUTED as transport for P>=64 | |
| 5 | 2026-07-28 | Collective-table gates are valid only cache-cold: cache-hit modules never re-dump HLO, so warm tables under-report | scorecard §AY | RULE | |
| 6 | 2026-07-28 | Flat-k MKL FFT (DFTI API) FFI: sigma.exec 272 -> 71.9 s (3.78x) at AQ 4962c/P=64; tau FFT trio 38.5x; HLO transposes 6 -> 0; unit gate 1e-16; h5 parity 2.5e-14 eV | jobs 7878708/7878719/7878727/7878745; repo commit 5918cf6 | GREEN — certified opt-in | |
| 7 | 2026-07-28 | Global k-leading relabel ("route b") would help the FFT layout | HLO probe job 7879370 (repo 068286c): batched minor-most FFTs move 0.00 MB; relabel pushes BSE into the costly layout | REFUTED | 6 |
| 8 | 2026-07-27 | `warm_mesh_cliques(mesh)` must run (x + y + world cliques) before the first jitted mpi collective; without it BSE TDA Lanczos dies with thread-main refusals (32 refusals at P=16) | failure job 7879458; discriminating controls 7881053; gate 7881216; enforcement `src/common/collectives.py::warm_mesh_cliques` | RULE | |
| 9 | 2026-07-28 | `check_hermitian` eagerly transposed the sharded (mu,mu) W tile, gathering 399 MB per operand per call; fixed with `with_sharding_constraint` (one fused jit was NOT enough) | repo test `test_check_hermitian_sharded_no_full_gather`; scorecard §AY | FIXED | |
| 10 | 2026-07-28 | Omega-cube fix (consume tiles where reduce-scatter left them, existing 2D mesh): gathers eliminated, byte-identical results | jobs 7878707, 7878722 | GREEN | |
| 11 | 2026-07-28 | Gram-build column-blocked fix removes the 98 GB single-node centroid Gram wall; byte-identical at c2475; mu~7k centroids fit 192 GB nodes | gate job 7878488 | GREEN | |
| 12 | 2026-07-28 | Memory model needs no recalibration: planner-vs-VmHWM 0.97-1.06 across ladder rungs 1-4 (walls 514/434/495/795/1217 s); memory wall extrapolates to mu~26k at P=64. `sacct` MaxRSS undersamples peaks; `/proc` VmHWM is authoritative | ladder record, scorecard §AY + `lorrax_setup/wk_REL/` ladder notes | GREEN | |
| 13 | 2026-07-28 | Intel MPI provider is `mlx` in-container; `FI_PROVIDER=tcp` only as the rtx escape hatch | `config/frontera/mpi_transport_env.sh`; scorecard | RULE | |
| 14 | 2026-07-28 | mu-convergence is non-monotone at 512b (-240/-183 meV c4951->c6947) vs +47/+38 meV at 256b | ladder record | OPEN — physics observation | |
| 15 | 2026-07-28 | `test_slate_cholesky_trsm_cpu` hangs; pre-existing, identical on the AW lib | scorecard | OPEN | |
| 16 | 2026-07-28 | zeta-apply performs a full-mu gather [1,4992,5760] under the distributed tier, O(mu*N_r/sqrt(P)) | scorecard | OPEN | |
| 17 | 2026-07-31 | fastloop mini-deck certified: full driver chain (kmeans->dipole->kin-ion->gw->eqp-convert->ht dft+qp) green at P=1 AND on a 4-host-device 2x2 mesh; p1 rerun exact-0 on every compared quantity, shard4 worst delta 1.15e-13 eV (sigma_mnk.h5); warm chain wall 76.6 s (p1) / 107.5 s (shard4); deck = MoS2 2x2x1 / 40b / 394c via the job-7884642 QE recipe | jobs 7884926 (deck), 7884936 (pin+check); `fastloop/pins_mini.json`, `fastloop/reference/`, src @ 24e4dc3 | GREEN — gate live, run before committing driver changes | |
| 18 | 2026-07-31 | `slab_io=auto` resolves to PHDF5_FFI whenever the host .so exports the handler, then aborts in MPI_Init_thread (MPIR_pmi_init) on a bare single-process launch with no PMI environment — auto does not probe MPI bootstrapability; mini-deck pins `slab_io=h5py_allgather` as the workaround; repo-side demotion fix reported, not patched | job 7884926 gw stage, `fastloop/work/build_minideck.7884926.out`; fastloop catch #1 | OPEN — repo-side fix needed | 21 |
| 19 | 2026-07-31 | `gw.gw_jax` completes main() (all outputs on disk, timing table printed) then hangs indefinitely in interpreter teardown on a bare P=1 launch (>=4 min silence observed); only chain driver affected (also the only one on the FFT/GEMM FFI paths); fastloop works around it with a post-main os._exit wrapper (`GW_WRAPPER`), the same pattern as the repo's failfast hook | job 7884928 (hang), 7884936 (wrapper green); fastloop catch #2 | OPEN — repo-side root-cause needed | 22 |
| 20 | 2026-08-01 | All seven chain drivers (gw_jax + kmeans_cli, get_dipole_mtxels, kin_ion_io, htransform, bse_jax, exciton_bands) routed through runtime.initialize_communicator_stack: one module-top startup call, zero direct prepare_mesh in drivers, canonical-mesh cache in collectives.resolve_mesh; numerically inert (fastloop check exit 0, parity exact-0 both legs except the known 1.15e-13 shard4 sigma_mnk) | job 7884986; repo e97e8ed; AST gates 34/34 + 68/68 + 9/9 | FIXED — subsumes 24e4dc3's interim routing | |
| 21 | 2026-08-01 | slab_io=auto probes MPI bootstrapability before either MPI tier (launcher PMI/PMIx env, else a throwaway-subprocess singleton MPI_Init probe that survives Intel MPI's abort) and demotes with a full announcement on bare launches; bare P=1 gw with slab_io=auto completes rc=0 on the demoted allgather tier, parity PASS; the mini-deck runs slab_io=auto as a standing regression test | jobs 7884987 (step 4), 7884989 (phases 0b/2); repo aef6710 | FIXED — closes 18 | |
| 22 | 2026-08-01 | gw_jax teardown hang root-caused: jax's atexit clean_up() destroys the XLA:CPU client and, after a fully-cold in-process compile storm (~195 XLA compiles), that destructor deadlocks in pool shutdown (main thread futex-parked, ~53 tf_XLAEigen workers spinning; Python atexit completes first). Deterministic in the fully-cold cell at BOTH 24e4dc3 and HEAD (2x 1200-s stage timeouts, thread census on disk); warm and small-cold runs clean in every FFI dial combination (6 runs). Fixed by runtime.finalize_process (ordered explicit teardown: effects barrier, unregister jax clean_up, distributed shutdown, remaining atexit hooks, announced os._exit) adopted by gw_jax __main__; fastloop check exit 0 with GW_WRAPPER REMOVED | jobs 7884986/7884987 (clean matrix), 7884989 (0a/0b hang repro + phase 2 acceptance); repo d3465cc; census fastloop/work/accept.7884989.d/hang_threads.004020.txt | FIXED — closes 19 | |
| 23 | 2026-08-01 | Replicated zeta-fit charge factor folded to a q-parallel P>1 SCHEDULE (same plan, same bits — deliberately NOT a third resolution; factor family stays replicated \| distributed): q scattered over all devices, whole per-q tiles, shared kernel `_charge_factor_math`, auto above nq·mu_log³ ≥ 5e9 (`LORRAX_ZETA_QPARALLEL` 0/1 override). b300 deck 8x2 A/B: zeta_fit.cholesky 104.372 → 11.755 s (8.9x; baseline 7884656 was 105.100 s), GW wall 335.9 → 214.6 s; parity max-delta 0.000e+00 on eqp0/eqp1/eqp_g0w0/sigma_diag/sigma_mnk.h5 for fold-vs-control, fold-vs-7884656 AND control-vs-7884656; bit-identity gate `test_qparallel_execution_is_bit_identical_to_replicated` max_abs = 0.0 (5 meshes × 2 modes, q-pad + mu-pad exercised); fastloop both legs PASS, mini-deck stays below threshold by design | jobs 7885021 (gates + fastloop), 7885024 (A/B, bundle 854af1f); repo 854af1f; harnesses + runs /scratch2/08271/jackmc/zeta_qpar/ | FIXED — closes KNOWN_LORRAX_ISSUES gw row 1 | |
| 24 | 2026-08-01 | Distributed zeta tier (`distributed_zeta_solve=distributed`, charge factor via ScaLAPACK pzheevd + 2D-sharded C⁺, back-solve one 2D-sharded GEMM) verified end-to-end on the b300 physics deck at the certified 8x2x28 / 4x4-mesh geometry: rc=0, GW wall 222.1 s; zeta_fit.cholesky 24.25 s (vs 11.76 s local q-parallel / 104.37 s all-ranks control, job 7885024 — expected slower at this size; the claim is correctness + memory shape), per-r-chunk back-solve 12.50 s vs 16.0 s local. Parity vs local tier AND vs 7884656 baseline: max\|Δ\| 8.573e-6 eV eqp0/eqp1, 9.000e-6 eV eqp_g0w0/sigma_diag, 4.936e-5 eV sigma_mnk.h5 — strict 1e-8 gate FAILS (block-cyclic eigh = different gauge, ~κ·ε with κ≤1/rcond=1e8, as documented), stated gauge tolerance 1e-4 eV PASSES, deltas identical against both references; truncation active at real conditioning (n_keep≈1995/2992 per q). Per-rank MaxRSS (sacct, sampled) 6.0 GiB vs 4.6 GiB all-ranks control — no O(nq·μ²) gather term; collective payloads: C⁺ site chunked 107.4 MB×4, GEMM site 485.2 MB/q with the documented ABANDONED-bound warning (loud floor, not a defect). Transverse channels: tier resolves to per_q by design; no standalone transverse factor stage exists — gap registered in KNOWN_LORRAX_ISSUES with design in repo docs/dev/large_nmu_operation.md | job 7885077; harness /scratch2/08271/jackmc/zeta_qpar/job3_b300_dist.sbatch; run run_b300_gw_dist; bundle 833af17 | VERIFIED | |
| 25 | 2026-08-01 | ζ-fit provenance records the `distributed_zeta_solve` GAUGE tier ('replicated' \| 'distributed'; per_q collapses to replicated — back-solve gather granularities over the same factor bits) with NO forced refit of existing ζ: schema stays 1, reader treats a MISSING key as legacy replicated (one-line notice, reuse allowed) and refits on real tier mismatch. Reader matrix on the REAL b300 legacy ζ: byte-identical→reuse; legacy+replicated→reuse+notice; legacy+distributed→refit with tier message; legacy+tier+changed-cutoff→refit. End-to-end (leg C, copied run dir): 2-key diff (tier + wfn_file) took the generic refit path with both keys named; refit outputs exact-0 vs control on eqp0/eqp1/eqp_g0w0/sigma_diag | jobs 7885105 (unit matrix + fastloop both legs PASS), 7885109 leg C; repo 8b02c72 | FIXED — closes KNOWN gw provenance row | |
| 26 | 2026-08-01 | Σ τ-loop async-D2H deque persists across window boundaries — one flush per branch, FIFO, bit-identical (unit accumulator matrix incl. a window shorter than lag: totals BIT-IDENTICAL to flush-per-window, sink order exact). b300 8x2 leg A vs job-7884656 baseline: EXACT-0 (0.000e+00) on eqp0/eqp1/eqp_g0w0/sigma_diag AND sigma_mnk.h5; sigma.exec 71.222→70.605 s; boundary flush 16 drains/5.77 s → 8 drains/2.64 s under sigma.finalize. CONFIRMS the stage is DEVICE-bound: BC's "88% d2h_wait" was the τ-kernel device wall surfacing in the drain (the ppm_accumulators comment warned this 2026-07-28), NOT missing overlap | jobs 7885105 (units + fastloop p1/shard4 exact-0 / known 1.15e-13), 7885109 leg A; repo 8a9a21c | FIXED — corrects the BC reading | |
| 27 | 2026-08-01 | Probe-χ₀ reuse `ppm_probe_chi_reuse=auto` (deck key, DEFAULT off): probe χ₀ accumulated inside ONE fused τ sweep on static + k augmented nodes drawn greedily from the dedicated quadrature (Lawson-weighted fits, gate = max(dedicated err, target), guaranteed exact-dedicated fallback). Weights-only refit on static nodes alone is provably insufficient: probe integrand = Laplace transform of cos(ωp t), static 1/x grid cannot resolve the τ-tail oscillation (2.6e-4 vs dedicated 1.3e-6). b300 8x2 leg B: fused 10+4 nodes (dedicated pass = 8), probe err 2.4e-6 ≤ gate 4.6e-6; chi0_W_probe 9.65 (BC) / 6.78 (control) → 3.56 s (chi.reused 0.0) — BUT greedy planning costs 1.79 s host, so screening NETS +1.2 s at b300 scale (16.16→17.34 s); pays only where per-node χ cost dominates the fixed planning cost (larger μ). Deltas vs control: eqp0/eqp1/eqp_g0w0/sigma_diag ≤ 8.7e-5 eV, sigma_mnk.h5 ≤ 1.039e-4 eV (marginally over a 1e-4 gate on off-diag Σ_total). Mini-deck P=1: probe row 0.592→0.122 s, eqp ≤ 1.49e-5 eV. Stays opt-in; default flip = owner call AFTER cheapening the planner + re-pinning baselines | jobs 7885097 (LS insufficiency), 7885105 (gate matrix ωp 0.5–8 Ry, k=2–5; mini end-to-end), 7885109 leg B; repo 64b17c3 | MEASURED — opt-in; no net win at b300 scale | |
| 28 | 2026-08-01 | htransform Galerkin factorization rewritten as Gram-eigh of A Aᴴ (nk·nb square, N_mu-free) through the ffi.linalg eigh plan (deck key `eigh_backend`; auto=native replicated, distributed=pzheevd), Vᴴ and B_at_mu mu-sharded on 'y' (B fitted at true n_mu); rank-0 writer gate on bandstructure.dat; Γ-label compared canonically (old literal-'\\u0393' comparison never matched; argmin fallback survived on the norm-0 pad-row tie). Gauge-class change, measured EXACT at every gate: fastloop p1+shard4 PASS with bandstructure dft/qp max\|d\|=0.0 both legs; b300 P=1 dft+qp AND srun P=4 (mlx, 1 node × 4 ranks) bandstructure.dat max\|d\|=0.000e+00 all 7 columns vs pinned baselines (gauge deltas below the 1e-8 print); rank 704/704 retained, identical to SVD. Memory (1 proc × 4 host devices, dft): mu 4962→18084 process-VmHWM slope +1.9 GB (ctl 18b1917) → +1.2 GB (new = exactly the sharded psi/Vh/B sum-of-shards terms; replicated A+gesdd term gone), wall 67-70 → 44-45 s; P=4 srun per-rank VmHWM 3.04-3.34 GB. Remaining mu-bound: single-axis psi-at-centroids sharding (centroids row, open). sqrt-of-eigenvalue precision note recorded at the cut (retained spectra measured 4.5 decades above it) | jobs 7885090 (fastloop), 7885093 (b300 matrix, harness /scratch2/08271/jackmc/ht_gram/job1_ht_gram.sbatch); repo 62ba395 | FIXED — closes KNOWN_LORRAX_ISSUES htransform rows 1-3 | |
| 29 | 2026-08-01 | Transverse ζ factor HOISTED (branch `zeta-transverse-hoist` @ f695bf4 on 5da517b; NOT merged — facade/C++ workstreams hold uncommitted edits on the same ffi files): `factor_c_q` runs the per-q ridged pivoted LU ONCE per channel and returns (factor, piv); `solve_zeta` only applies it per r-chunk. LOCAL plan = `lax.linalg.lu` on the whole logical tile (q-parallel under the charge fold policy) + `jax.scipy lu_solve` — a SCHEDULE, bit-identical to the fused per-r-chunk `jnp.linalg.solve`: unit gate `test_transverse_factor_hoist` EXACT (3 meshes × 2 gather tiers × 2 schedules × 2 r-chunks, q-pad + μ-pad, TRS near-null fixture) AND production bispinor A/B EXACT-0 on eqp0/eqp1/eqp_g0w0/sigma_diag/sigma_mnk.h5 at bi4 (402c+143T, P=4/2×2) and bi16 (785c+275T, P=16/4×4). Controls also reproduce the archived AX baselines: exact-0 .dat, 1.137e-13 eV h5 (merged branch + certified FFT-FFI stack vs wt-BC env). Measured: bi16 per-channel zeta_fit.chunk.solve 4.3–12.8 s → 1.7–2.8 s (re-factorization gone), GW wall 214.3 → 201.2 s; scales as n_rchunks·P at production μ_T. cusolvermp (CUDA) keeps the fused pair (port pending; fused branches preserved under lu_piv=None) | jobs 7885122 (unit gates 10 passed incl. 2 new), 7885126 legs A/B/C/D; fastloop both legs PASS 7885123 (worktree src + new .so) | FIXED — closes KNOWN gw transverse row | |
| 30 | 2026-08-01 | ScaLAPACK LU pair SPLIT: new host handlers `lorrax_scalapack_batched_getrf` (block-cyclic factors + per-rank int32 ipiv out) / `_getrs`; hoisted distributed transverse factor runs pXgetrf once per channel, pXgetrs per r-chunk with ipiv threaded verbatim (never gathered). Split-vs-fused BIT-IDENTICAL: contract cell `scalapack_getrf_getrs` exact (incl. factor reuse across two RHS) on the 2×2 process mesh (impl=mpi, both dtypes; 1×1 pytest form dies silently at import with the .so present — pre-existing bare-launch MPI-init class, CLI matrix is the instrument) and production E/F legs (402c+136T, P=4) EXACT-0 on all five outputs. Distributed-vs-local gauge (F vs G): 0.0 at .dat print precision, 1.137e-13 eV sigma_mnk.h5 — far under the 1e-3 eV significance bound at this size. Resolve now probes all three handlers (old .so refuses, rebuild build_ffi_host.sh); .so build_host_TLU sha 5e0ba705…, symbol table = ONE + the 2 new handlers. CLI entry fixed for cpu+impl=mpi (eager slate MPI warm-up now CUDA-only — double MPI_Init abort, 7885123); post-verdict "MPI routine after finalizing" teardown rc=1 in CLI mode remains (cosmetic, verdict line is the record) | jobs 7885123/7885125 (build + CLI matrix "done: 0 failures"), 7885126 legs E/F/G; repo branch commits 6571c04, 2fca4b4 | FIXED | |
| 31 | 2026-08-01 | Mesh-divisible transverse centroid set for the 4×4 deck: `centroids_T_div136.txt` (136 pts = %8==0 — divides 2×2, 4×4 and 8×8 mesh axes; kmeans_cli 130 --orbit --density-mode current --qe-save out/MoS2.save --seed 0, same provenance header as centroids_T_t134), at /scratch2/08271/jackmc/zeta_T_hoist/. Ladder observation: requested 128-138 delivered {133,136,138,141,145} — orbit closure makes exact divisibility a search, ~1/6 hit rate at this size. The ~272-pt divisible set (bi16-scale scalapack) NOT yet generated — deferred. Deck-dir placement deferred (mos2_4x4_test treated read-only, in use by another agent) | job 7885126 step -1; job 7885122's ladder invalidated (leaked JAX_COORDINATOR_ADDRESS → kmeans joined the CLI leg's coordinator, DEADLINE_EXCEEDED; job cancelled) | GREEN | |
