# runs/

Calculation decks and their outputs live here, one directory per run, each with a `manifest.yaml` (template in `templates/manifest.yaml`). Purged 2026-07-31; the Perlmutter-era contents (154,951 files) remain in git history at commit `6324f27a`.
