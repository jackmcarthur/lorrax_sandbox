run_id: cri3_c3_covar_t206_2026-06-16
system: CrI3
pipeline: lorrax_only
platform: perlmutter

variant_of: cri3_c3_full_bz_ref_2026-06-16
reuse_from_parent: [qe/, qe/nscf/WFN.h5, centroids_frac_300.txt]
overrides:
  purpose: "Transverse-centroid convergence of the in-plane TT C3 covariance violation"
  transverse_centroids: 206  # orbit-closed, --density-mode current --seed 42
  ibz: "full-BZ FORCED via LORRAX_FORCE_FULL_BZ=1 (covariance reference leg)"
  charge_centroids: 300

steps:
  x_only: { state: complete, note: "V_q (full BZ 36 q) written; benign write_results IndexError after; max in-plane rel-eig spread = 0.255" }

# Convergence series with C_cri3_full_bz_ref_2026-06-16 (102): 102->0.186, 206->0.255, 308->0.087, 410->2.49.
# Charge-tr & z-z spread ~1e-8 at every count (alignment good); in-plane NOT covariant, grows with centroids.
