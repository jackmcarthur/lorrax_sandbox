run_id: mos2_3x3_scalar_cohsex_screened_2026-06-15
system: MoS2
pipeline: lorrax_only
platform: perlmutter

variant_of: mos2_3x3_D_60Ry_bispinor
reuse_from_parent: [qe/, WFN.h5, centroids_frac_640.txt, kin_ion.h5]
overrides: {bispinor: false, x_only: false, do_screened: true}

# Purpose: scalar reference for the milestone-A charge-block check — same screened-charge
# COHSEX (Σ_SX+Σ_COH on W⁰⁰) as the bispinor run but bispinor=false (no Σ^B). The bispinor
# run's Σ_SX/Σ_COH must equal these (holds by construction; cohsex_sigma.py:211-212).
# Requires LORRAX_FORCE_FULL_BZ=1.

steps:
  00_lorrax: { state: pending, note: "blocked by concurrent 16-GPU pool job; patient retry queued" }
