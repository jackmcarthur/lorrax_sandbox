"""Staged GW features that aren't yet wired into the production
``gw.gw_jax.main`` flow but pass their own targeted tests.

Modules here are real, tested, and intended for promotion — not
abandoned code.  Move them up to ``gw/`` proper when the production
driver consumes them.
"""
