# BSE program PLAN — revised 2026-07-15 (supersedes MAP.md §7)

_Revision incorporates Jack's directives (2026-07-15): fix broken areas first;
V/W recover-or-generate from restart with loud warnings; BSE infrastructure as
close to the GW stack as possible (phdf5/tagged_arrays/gw_config/SymMaps);
large-scale consolidation mindset — forest, not trees. End goal of this arc:
**reproduce BGW BSE calculations** on fresh restarts._

## Standing mandates (bake into every agent prompt)

1. Infra proximity to GW: config via `gw_config.read_lorrax_input`; I/O via the
   tagged_arrays/mf_header-binder/slab-IO (phdf5) machinery; padding via
   `runtime.padding`; symmetry via ONE `SymMaps` table. Larger consolidations
   than the assigned change go in `CLEANUP_LOG.md` "Deferred consolidations".
2. No redundancy; procedural style; no new class hierarchies.
3. Feature branch `agent/bse-cleanup` (worktree
   `sources/worktrees/lorrax_D_bse_cleanup` — lorrax_D's own tree is in use by
   another session). Never main; no pushes without instruction.
4. No bare python on login nodes (lxrun/srun via lorrax_agent pool).
5. Gates run on 1 GPU, MoS2/Si-scale fixtures.

## Phase 0 — adjudications ✅ SETTLED

**B1 exchange kernel: DENSE in (k,k′)** (2026-07-15, unanimous 3-agent
adversarial round incl. a failed k-diagonal steelman + BGW bsemat.h5 empirical
dump — `archive/adjudication/VERDICT.md`). Jack's momentum-shift-0 statement
confirmed for the interaction line (q=0/Q tile only); the δ_kk′ belongs to the
direct term. Finite-Q: H is Q-block-diagonal (±Q = pair-anchor convention);
within a block, dense; v(Q+G) keeps G=0. The fix is cheaper than the bug
(k-summed encode `'kcvN,bcvk->bN'`, broadcast decode) and makes Q=0 the Q→0
case of one unified exchange path.

## Phase 1 — fix broken areas 🔄 IN FLIGHT (`agent/bse-cleanup`, workflow bse-cleanup-phase1)

Repairs: B3/B4/B5 loader repair (single-sourced layout normalization to flat-q,
kgrid pass-down, ONE head-injection helper), B2 non-TDA einsum, B6 padding
(+eps-pad guard), B7 eqp-window single-sourcing, KPM phantom kwarg,
bse_pseudopoles lost-wiring restore, **W0_ready loud warning** (Jack's spec),
STATUS.md/docstring drift. Then the verified Tier-F delete pass + sweep-harness
archival. Runtime checks via GPU pool when reachable; PENDING logged otherwise.
Deliverables: commit series + `CLEANUP_LOG.md`.

## Phase 2 — B1 dense-exchange fix + the gate wall

1. Dense-reference kernel gate: 2-k toy + the N=144 (4v4c 3×3×1) exact fixture;
   **plus direct comparison against BGW `/mats/exchange`** (layout mapping
   documented in `archive/adjudication/empirical_bsemat.md`).
2. B1 fix as its own commit against those gates. Expect singlet/bright-state
   shifts; Si triplet manifold ~unchanged; re-freeze anchors deliberately.
3. Fresh-restart e2e pytest gate (1 GPU): gw_jax `do_screened` on the MoS2
   regression fixture → BSE lowest-n_eig on the restart it just wrote
   (this class of gate would have caught B3-B5).
4. Pad-mask gate (non-divisible band counts, 2×2 mesh).

## Phase 3 — recover-or-generate V/W (scout in flight → implement)

Per Jack: BSE preferentially reads V_qmunu/W0_qmunu (+eqp) from the restart;
missing pieces are **generated by calling the gw stages as library functions**
(kmeans→fit_zeta→compute_all_V_q; compute_screening→persist_w0_and_head) behind
one `produce_restart_if_missing()` seam — with a big banner warning that no
restart was found, and a separate warning when falling back to DFT energies.
No duplicated GW logic in bse/. Wiring plan:
`archive/designs/recover_or_generate_vw.md` (scout deliverable).

## Phase 4 — single-source + GW-infra alignment sweep

Remaining redundancy (pair-amplitude ×3, T-encode ×2, eigvec writer ×2, restart
loader ×2), config through gw_config everywhere, extract
`create_mesh_2d`/`make_bse_shardings` to a C-tier home, then the alignment
audit: which bse_io reads should ride the GW phdf5/slab-IO stack, shared
mf_header binders, one padding convention. Act on the "Deferred consolidations"
ledger accumulated in Phases 1-3.

## Phase 5 — solver program (archive/designs/solver_program.md P1→P3)

P1 consolidate+unblock → P2 FEAST maturation (Zolotarev, cache keys, W_R-once)
→ P3 clustered eigenvalues (DOS-sized subspace, stochastic tr(P) count,
locking). Net −800…−1000 L.

## Phase 6 — features (dependency order)

coulomb_sr_lr P1 + w_head_wings P1 (behaviour-preserving) → fine-grid
interpolation P1-P3 → finite-Q on-grid (unified with the dense exchange path
from Phase 2) → arbitrary-Q / non-TDA / 2D-CSI per Jack's pending decisions
(report.md "Decisions needed").

## Milestone — reproduce BGW BSE calcs

Fresh QE→BGW→LORRAX runs per skills (build_inputs → execute_workflow →
compare): Si 4×4×4 8v8c first (re-anchor eigenvalues + Σ|d|² + Haydock overlay
on FRESH restarts, post-B1 — singlet states will move vs the old validation),
then MoS2. BGW_COMPARE.md's six conventions apply verbatim.
